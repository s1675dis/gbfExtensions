# 新セッションへの引き継ぎ

## v1.13.130: 導本ページによる全所持効果の完全同期

- `#arcarum3/book`を開くと、ユニーク・レア・ノーマル・呪われた導本の4タブを自動的に順番に表示し、各カテゴリの所持効果を収集して元のタブへ戻す。
- 導本ページは所持中の効果だけを表示するため、`×n`表記がない行は`count: 1`として扱う。従来の単数所持が「所持数不明」になる問題を修正した。
- 4カテゴリすべての読取完了時だけ`guidebook_full_sync`を発行する。不完全な巡回結果では不在効果を0へしない。
- 完全同期では表示された効果の`count`を置き換え、全カテゴリに存在しない既知導本の`count`を0へする。名称、ID、レアリティ、重複可否、観測・獲得履歴は保持する。
- 完全同期は所持状態の照合であり獲得イベントではないため、`acquisitionCount`、直近獲得ハイライト、取得トーストを増やさない。
- 完了時は導本タブの状態欄へ「導本ページと完全同期 / 所持効果 n件」を表示する。
- ページを離れると訪問完了フラグを解除し、次に導本ページを開いた際に再同期する。

## v1.13.129: 探索開始時の価値連動型外周展開

- 第一次収縮先が未確定の`T0～T8 / 1日目 / 瘴気未発生`を`initial-outer`フェーズとして扱う。
- 浮遊城本体を除くMAP座標の外接範囲中心を求め、中心からの距離をMAP最大半径で割って各nodeの外周度を0～1へ正規化する。
- 候補比較へ、取得価値、価値nodeの外周度、開始地点からの最大外周進展、初手の外向き進展、内向き移動量、終点から3ホップ以内にある外周側未訪問価値を統合する。
- 外周側の将来価値は距離で減衰し、終点より明確に内側の価値は0.15倍にする。外周に出た後も価値が連続する枝を優先し、単なる外周空白や袋小路を目的にしない。
- 瘴気、先頭価値取得遅延、空白、折り返しの制約が同等な候補間で適用する。宝箱など明確に高い価値は外周方向より優先する。
- UI見出しは「探索序盤・価値回収＋外周展開」。Codex報告にも適用状態と終了ターンを含める。
- T9以降または瘴気発生後は従来の第一次収縮期限・安置外回収・退避アルゴリズムへ移行する。

## v1.13.128: start_dungeonをルート最適化全体のセッション境界に統一

- `start_dungeon`成功時に`routeSessionId`とgenerationを発行し、ルートキャンバス・推奨手順へ`GBF_ROUTE_SESSION_STARTED`を通知する。
- 前探索のnode、現在地、実座標、消化済みnode、危険通過済みnode、転移拒否、浮遊城復帰位置、特殊イベント進入中状態、1日目ボス討伐、通貨、イベントアイテム、パーティHP、瘴気、収縮ダメージ、第一次収縮円を空状態へ戻す。
- サイドパネルは通知受信時に両キャンバス、推奨手順、収縮指標、コピー用レポートを即時破棄し、「新しい探索のMAP初期状態を待っています」と表示する。
- 新セッション待機中は`GBF_GET_ROUTE_STATE`が旧ページを再取得せず待機状態を返す。遷移中に遅れて届いた前探索のfield captureも破棄する。
- `T0～1 / node複数 / 訪問済み1件以下`の新MAP初期状態、または同条件の`content/index` Ajaxを取得した時だけ待機を解除し、ルート最適化を再開する。
- 非同期の収縮学習処理にセッションgenerationを渡し、前探索で開始した処理が新探索の収縮円やモデルを書き戻すことを防ぐ。
- 固定特殊イベント座標辞書、表示半径設定、全探索共有の第一次収縮学習モデルは維持する。

## v1.13.127: start_dungeonによる新規探索確定

- 実測ログは`retire_dungeon`成功後、編成選択を経て`/rest/arcarum3/start_dungeon`を呼ぶ。成功応答は`redirect_scene`と`tutorial_info`を持つ。
- 直後の`/arcarum3/dungeon/content/index/0`は`map_id: 3 / current_node_id: 1 / total_turn: 0 / 訪問済みnode 1件 / 通貨0`だった。
- `start_dungeon`成功を新規探索の一次判定に追加した。導本辞書の現在所持数`count`を0へ戻し、タブ別探索マーカー、前探索のルートランタイム、第一次収縮円、重複学習署名を破棄する。
- 導本の定義・ID・レアリティ・累積観測履歴と、全探索共有の第一次収縮学習モデルは保持する。
- 従来のターン・訪問済みnode巻き戻り判定は、開始Ajaxを取りこぼした場合のフォールバックとして維持する。
- 同名alarmをService Worker起動ごとに作り直して定期更新時刻を延期しないよう、既存alarmがある場合は再作成しない。

## v1.13.126: GitHub経由のオンライン更新

- `s1675dis/gbfExtensions`の`update/channel.json`を起動時・インストール時・6時間ごと・手動操作時に確認する。
- 導本辞書と第一次収縮の共有モデルは固定したraw GitHub URLからJSONだけを取得する。任意URLやリモートJavaScriptは実行しない。
- 導本辞書は既存のインポート統合を利用し、探索中の`count`、取得履歴、既知メタ情報を保持する。収縮モデルはサンプルを重複排除してローカル学習値と統合する。
- 拡張本体の新バージョンは数値比較し、サイドパネル上部へGitHub Releasesの「配布ページ」ボタンを表示する。unpacked拡張自身によるコード書換えは行わない。
- `.github/workflows/release.yml`はmainで単体テストとZIP生成を行い、`v*`タグではGitHub Releaseを作成する。
- 更新手順とChrome上の制約は`docs/ONLINE_UPDATE.md`へ記載した。

## v1.13.125: 撤退後の新規出撃で獲得導本効果をリセット

- 導本辞書内の`count`は現在の探索だけの所持数だが、従来は撤退後も永続レコードに残り、「獲得導本効果」が前探索のまま表示されていた。
- タブごとのMAP進捗を`guidebook-sortie-state:v1:<tabId>`へ保存する。全MAP取得時にターンが0～1へ戻る、または複数の訪問済みnodeが開始地点1件へ戻る場合を新規出撃と判定する。
- 新規出撃時は導本レコードの`count`だけを0へ戻し、`guidebook-effect-values:v1`を即時再生成する。名称、ID、レアリティ、解析結果、候補・獲得の累積履歴は保持する。
- 同じ探索のページリロードではターン・訪問済み集合が巻き戻らないためリセットしない。
- ルートAjaxと250msフィールド再同期の両方から進捗を観測する。Service Workerが停止してもタブ別進捗マーカーは`chrome.storage.local`から復元する。

## v1.13.124: 第一次収縮円の初期推定と自己学習

- 未観測map/patternでもシミュレーションを止めず、`limitCircle.position`を画像左上、半径を670として最終円を仮置きする。左上が取れない場合は取得済み円中心、サーバー中心、MAP重心の順で補完する。
- 第一次収縮完了時（level 1 / status 2またはstep 100）、`isShrinking: false`の安全nodeと`true`の瘴気nodeを最もよく分離する円を自動フィットする。安全側最遠点と瘴気側最近点の中間を境界半径にする。
- 学習モデルは`mapId:basePatternId:patternId:dayIndex`単位で`chrome.storage.local`の`route-first-shrink-models:v1`へ永続保存する。タブ終了やChrome再起動後も利用する。
- 最大20観測の中心・半径・開始半径を中央値で統合する。同一タブの同じ境界は30秒間重複登録しない。
- 使用優先順位は「自己学習モデル → 既存の登録済み実測モデル → 円画像座標による初期推定」。学習完了時はタブの第一次最終円スナップショットも即時置換し、第2収縮の開始円へ渡す。
- `page-hook.js`の250msフィールド再同期と全arcarum3 Ajax更新から自動学習を起動する。オペレーターによる保存・報告操作は不要。

## v1.13.123: 獲得導本効果を現在所持中だけに限定

- 「獲得導本効果」は定義済み項目を0付きで並べず、効果確認ページの`count`で現在所持が確認でき、集計値が0ではない効果だけを表示する。
- `count: 0`の未所持導本と、所持数未確認の導本は有効効果へ表示・加算しない。所持数未確認件数はステータス行だけに残す。
- 割合、追撃、固定値、導本個数、回数、特殊効果の全区分へ同じ条件を適用する。有効効果がない区分は「有効な効果なし」と表示する。
- ここでいう有効は「現在所持が確認できる効果」を指す。戦闘中の条件付き効果が今まさに発動条件を満たすかの判定はまだ行わない。

## v1.13.122: 第一次収縮完了後の外周→内周回収

- 提供報告はmap 2 / T32 / current node 68 / 第一次収縮status 2・step 100。旧推奨は`68 → 69`だった。
- node 58と69は隣接する同価値イベントだが、58は最終安置中心から約663、69は約492。`68 → 58 → 70 → 69 → 2…`なら外周から内側へ抜けられる一方、69を先にすると58が後の袋小路側に残り、`70 → 58 → 70`の折り返しが必要になる。
- 第一次収縮完了時、同じ安全性・瘴気損失・回収価値・空白コスト・折り返しコストの候補間では、最終安置中心から遠い価値nodeを先に選び、外周から内周へ掃引する。
- 真の袋小路やbridge edgeには外周優先を付けない。安全性や価値が異なる候補にも適用しないため、外周距離が宝箱・期限・瘴気制約を上書きしない。
- 添付の完全マップを再投入し、修正後が`68 → 58`になることを確認済み。

## v1.13.121: 導本効果計算の全ランク対応と追撃枠分離

- 提供`gbf-guidebook-effects-2026-07-24T05-43-49-172Z.json`は99件。ID 1～128の実名称・rarity・重複可否・countを基準に解析規則を拡張した。
- 百分率はエンハンス、攻撃、自属性攻撃、通常与ダメ、連撃、アビ3種、奥義5種、CB3種、ダメ上限、クリティカル、HP、防御、被ダメ軽減、回復、コイン、瘴気軽減を分解する。
- 固定値は被ダメージ減少・再生、個数は渾身・背水、回数は召喚可能回数・通常攻撃ヒット数として単位を分離する。
- 「敵属性に関わらず弱点」「全武器種」「全種族」「有利属性変換」は、慧眼効果・全武器種化・全種族化・自属性変転へ読み替える。
- ID 62は攻撃力100%、ダメ上限50%、命中率-25%の3効果へ分解する。
- 追撃は同じ導本ID内だけ所持数分を加算し、異なるIDは`slotKey`で別枠表示する。種族・得意武器・HP条件・地帯・攻撃回数条件はラベル接頭辞で区別する。例: ID 18は自属性奥義追撃、ID 36は種族自属性奥義追撃。
- `acquisitionCount`は全期間の取得観測累計なので、現在所持計算には使わない。効果確認ページの`count`が取得できた効果だけを合計し、未確認効果は0として「未確認」に数える。
- UIは「獲得導本効果」として割合効果、固定値・個数・回数、特殊効果の3区分を動的表示する。

## v1.13.120: R1導本の効果値抽出と獲得効果集計

- R1の効果本文から、攻撃力UP・連撃率・アビ与ダメ・アビダメ・アビ上限の百分率を抽出する。
- 元の導本台帳`guidebook-effects:v1`は変更せず、数値化した結果を`guidebook-effect-values:v1`へ別保存する。各レコードは元ID/key/name、重複可否、所持数、基礎値、所持数反映後の`totalValue`を持つ。
- 効果確認ページ由来の`count`がある場合は現在所持数として優先し、未取得時は実獲得記録`acquisitionCount`を暫定値にする。`isDuplicationPossible: false`または本文中の「重複不可」は所持数にかかわらず1個分まで。
- 導本リスト上部へ「R1獲得効果」を追加し、攻撃力UP・連撃率・アビ与ダメ・アビダメ・アビ上限の合計を2列のコンパクト表示にする。
- 現段階は単純な百分率強化5種のみ。渾身・背水・クリティカル、条件付き効果、固定値、R2/R3、呪われた導本は未集計。

## v1.13.119: 浮遊城本体の孤立判定とランダム戦闘特殊の分離

- 浮遊城本体は2候補座標からランダムに出現するため、既知の`XY 346,292 / specialType 4`に加え、`nodeType 10`で送出edgeも他nodeからの流入edgeも一切ない完全孤立nodeを浮遊城本体と判定する。
- 完全孤立判定したnodeは、未知の2箇所目でも実データ登録を待たず、経路グラフ・目的地・退避先・期限付き価値・特殊イベント座標記録から除外する。一時的に孤立nodeだけが返る演出中も、直前の浮遊城転移nodeを有効現在地として維持する。
- 狂信者（specialType 2/3）と邪教祖（specialType 1）は完全ランダム出現なので固定座標の特殊イベント記録から除外する。既存保存分も次回収集時に削除する。
- 狂信者・邪教祖は通常戦闘相当＋小さな追加報酬として評価する。全狂信者処理などの固定連鎖条件は使わず、その先へ空白を挟まず別の未処理価値nodeが連続する場合だけ候補にする。袋小路や空白先の報酬を理由に積極的な寄り道は行わない。

## v1.13.118: 転移拒否を推奨手順の転移ノードへ統合

- 転移しない判断を、推奨手順末尾の独立した「💎 転送しない」行やモード見出しの補足として表示しない。
- 現在地の転移nodeを推奨手順へ含め、そのnode名称自体を`🚫転移（転移しない）`へ変更する。
- 詳細欄はnode ID・XYと`現在地・転移を拒否`を表示する。後続の移動手順は次の番号から続ける。
- 転移拒否nodeは赤いステップ色にし、通常の転移nodeおよび「転送する」判断は従来表示を維持する。

## v1.13.117: 第一次収縮中の長い空白先行を抑制

- 添付報告（map 3 / T10 / current node 57）では、隣接する未訪問イベントnode 51を無視し、`57 → 62 → 1 → 84 → 85`と空白4マスを先行してから期限付き報酬へ向かっていた。
- 原因は、第一次収縮中の候補比較で期限付き報酬合計を最上位に置き、探索上限10 step内へ期限付きイベントを1個多く入れるためなら、最初の価値取得が大幅に遅れる経路を許していたこと。
- 候補へ`firstValueArrival`を保持し、最初の価値マスの処理完了まで4ターンを超える部分を`leadingValueDelay`として、期限付き報酬合計より先に抑制する。
- 4ターン以内の差は従来の期限・緊急度・宝箱・空白・効率比較へ委ねるため、早い期限の戦闘より1ターン近いだけのイベントを誤優先しない。
- 実報告を再計算すると、先頭は`57 → 51（イベント）→ 57`となり、その後に期限付きのイベント・戦闘・宝箱回収へ復帰する。
- 「隣接イベント」と「空白4マス後の期限付きイベント」の最小回帰シナリオを追加した。

## v1.13.116: 固定特殊イベント14を登録

- `event 14 / XY 910,1419`をMAP・node ID非依存の固定座標イベントとして登録した。初回実測はmap 2 / node 15。
- 内容は強敵1戦。勝利後に、仲間（通貨300相当の価値）または最高レアリティ導本を選択できる。
- `treatAsDanger: true / battleEquivalent: 1 / minimumProcessingTurns: 1`として、移動とは別に最低1戦闘ターンを見積もる。
- 1日目は強敵と同じく`firstDayValue: 0`で積極攻略しない。1日目ボス撃破後は`priority: day-two-very-high / value: 2`として、どちらの報酬でも通常戦闘より高く評価する。
- 登録済みイベント一覧から除外され、ルートMAPでは消化後に「済」表示の対象となる。

## v1.13.115: ボス位置の強調と観測境界からの第2収縮再開

- 収縮シミュレーションMAPで、未討伐のボスnodeをマゼンタの円・点線halo・ドラゴン絵文字で強調表示する。tooltipにもボス座標と収縮アンカーであることを表示する。
- `appearBoss.targetNodeId`または`node_type: 1`と座標を取得した時点で、第2収縮の正式なlevel/status更新を待たずシミュレーションを開始する。残りターンがまだ無い瞬間は第2収縮の既定20ターンを使用する。
- 保存済み`firstShrinkFinalCircle`がある場合は従来どおりそれを最優先する。
- 保存円がない場合でも、安全node（`isShrinking: false`）と瘴気node（`isShrinking: true`）が両方あれば、両者を最もよく分離する現在円を復元する。サーバー中心で矛盾なく分離できる場合はその中心を優先し、矛盾する場合は中心と半径を探索して分類誤差を最小化する。
- 復元した円を「現在時点の開始円」とし、残りターンにわたってボスを固定アンカーとする相似縮小を再開する。復元もできない場合だけ接続距離フォールバックを使用する。
- ボス座標先行、観測境界復元、従来の保存円による相似縮小を単体テストへ追加した。

## v1.13.114: pattern 1:1の最終安置円を実測境界へ補正

- map 4 / base pattern 1 / pattern 1の目視境界は、node 37 `(212,631)`が円内、node 70 `(1550,675)`が円外、node 119 `(1064,1267)`と120 `(1180,1216)`が縁のすぐ外側。
- この4点は登録済みモデルの中心`(843,629)`・半径`650`と一致する。画像外形由来の中心`(893,729)`・半径`670`とは一致しない。
- `miasma_circle_1.png`の1340px外形は描画アセットの情報であり、登録済みpatternの内側安全境界を上書きしない。patternモデルがある場合は、中心・最終半径ともモデルを優先する。
- 既存タブに`source: saved-native-image:*`の誤ったスナップショットが残っていても、登録モデルが利用可能なら次回の最適ルート表示時にモデル円を再保存する。
- 未登録patternだけは、従来どおり画像座標をフォールバックとして保持する。

## v1.13.113: event 16の1日目優先度を低下

- 固定特殊イベント`event 16 / XY 1597,1062`は強敵との連戦になる可能性があるため、`firstDayValue: 0 / treatAsDanger: true`へ変更した。
- 1日目は通常の強敵系と同じ空白相当で、通常戦闘・通常イベントより優先して目的地にしない。ただし、その先に価値がある場合の通過までは禁止しない。
- 1日目ボス撃破後は従来の`value: 1`へ戻る。最低2処理ターンと、アイテム有無に関する既存仕様は維持する。

## v1.13.112: フィールド復帰後のnode種別欠落と誤空白化を修正

- 添付ルート報告は全153 node中、空白75・通常イベント30・通常戦闘16。60秒後の同じ`Game.view.data`は空白57・通常イベント41・通常戦闘22で、19 nodeの種別が報告側だけ空白化されていた。
- `route_field_resync`の重複署名が従来はnode数しか含まず、復帰直後の空白中心nodeListから、後続のtype確定nodeListへの変化を検知できなかった。
- 署名へ全nodeの`id / type / specialType / isVisited / isShrinking`を追加した。node数・現在地・turnが同じでも種別確定時に再送する。
- reportで訪問済み扱いだったnode群は複数の非連結区画へ分散し、物理的な移動経路として成立していなかった。
- 原因は`move_node`以外のarcarum通信で`current_node_id`が一時変化した場合まで、以前のcurrent nodeを通過・消化済みとして推定していたこと。
- `inferConsumedRouteNode()`の自動実行を`move_node`応答だけへ限定した。実移動で通過した通常戦闘・通常イベント等を空白化する従来仕様は維持する。
- 非移動通信でcurrent nodeが1→2に変わってもnode 1のevent type 5を保持し、`inferredConsumedNodeIds`へ追加しない回帰テストを追加した。

## v1.13.111: 戦闘中に始まった第1収縮をフィールド復帰時に再同期

- `page-hook.js`が`#arcarum3/dungeon/`のフィールド状態を250ms間隔で監視する。
- 全ノードと現在地が取得できた時点で`route_field_resync`を送信する。署名にはMAP、現在地、現在ターン、node数、収縮active/level/status/step/remainTurn/pattern/center/limitCircleを含め、同一状態は重複送信しない。
- `pageshow`と非表示解除時にも再同期を試行する。
- backgroundの`updateRouteRuntimeFromFieldCapture()`は、戦闘前runtimeより復帰後ページ状態を優先してmergeする。これにより戦闘中に進んだ収縮段階、ターン、全node座標を復元する。
- `GBF_ROUTE_STATE_UPDATED`を送るため、「最適ルート」タブ表示中は既存のrefresh経路で即時再計画し、`ensureFirstShrinkCircleSnapshot()`が第1収縮円を生成・保存する。
- テストでは戦闘突入時の座標なしruntimeから、復帰後のturn 12 / level 1 / step 30 / remainTurn 15 / 座標3nodeを再同期し、pattern `*:1:1`の最終円`center (843,629) / radius 650`を生成できることを固定した。

## v1.13.110: miasma_circle_1画像の実寸から最終円を確定

- 提供画像`miasma_circle_1.png`は`1340×1340`、中心`(670,670)`。
- `limitCircle.position`を画像左上とし、`position + (670,670)`を画像由来中心とする。
- 画像由来中心が`center_position_x/y`と各3px以内で一致する場合だけ、半径670を採用する。表示倍率違い・別画像の誤採用を避けるため、ファイル名だけでは確定しない。
- 実測`position (223,59)`では画像由来中心`(893,729)`となり、サーバー中心と完全一致する。
- 確認済み画像円は`circleGeometrySource: native-image:miasma_circle_1.png`として、第1収縮のモデル値より優先して`firstShrinkFinalCircle`へ保存する。
- 第2収縮はv1.13.109の仕組みにより、この保存済み中心・半径を開始円として流用する。

## v1.13.109: 第1収縮の最終円を保存して第2収縮へ流用

- `createFirstShrinkCircleSnapshot()`で第1収縮モデルの`finalCenter`と`finalRadius`を一度だけ確定する。
- スナップショットは`mapId / basePatternId / patternId / dayIndex / capturedAt / modelKey`を伴い、`routeRuntimeCache`と`chrome.storage.session`へタブ別保存する。
- サイドパネル再読込やservice worker再起動後も、同じタブの保存済み円を復元する。
- 第2収縮の座標シミュレーションは`firstShrinkFinalCircle`だけを開始円として使用する。`simulateFirstShrinkArea()`の再実行、現在の`centerX/Y`・`circleRadius`、node範囲からの推定は行わない。
- 保存済み円がない場合は別の座標円を作らず、`boss-anchor-hop-contraction`へフォールバックする。
- 日・MAP・patternが一致しないスナップショットは使用しない。MAP変更時は保存値を破棄し、2日目は`dayIndex: 2`として新しい第1収縮円を保存する。

## v1.13.108: 強敵3連戦event 17

- 固定イベント`event 17 / map 4初回実測 / XY 1543,1331 / node 18`を登録した。識別はmap/nodeに依存しない。
- `treatAsDanger: true`、`battleEquivalent: 3`、`minimumProcessingTurns: 3`。
- 1日目は`firstDayValue: 0`で強敵と同じ空白相当。通常戦闘より優先しない。
- 1日目ボス撃破後は`value: 3`へ昇格し、強敵・超強敵を含む強敵系nodeより優先する。

## v1.13.107: 第2収縮の進捗を専用countdownへ修正

- 第2収縮で第一次収縮用の`miasma.step`を流用しない。
- 進捗は`(20 - remainTurn) / 20`。`step: 100`が保持されても即時収束せず、`step`欠落時にも停止しない。
- 第一次最終円を開始円、boss nodeを固定アンカーとし、半径と開始中心―bossベクトルを同じ残り比率で縮小する。
- 実験MAPへ開始円の白い点線、現在円の水色線、開始中心からbossへのピンク点線を表示する。
- Codex報告へ`progressSource: remain-turn`と`durationTurns: 20`を追加する。

## v1.13.106: 孤立報酬の先にある消化済み区画を入口で評価

- 最初の価値マスを回収した後、次の未回収価値マスまでに必要な訪問済み・空白マス数を`continuationGap`として候補へ加算する。
- 単純なホップ数ではなく、回復などの有効な中継マスは空白コストに含めない。
- 2つ目の価値マスまで到達した経路は孤立報酬ではないため、このペナルティを解除する。
- 添付のmap 3 / current node 7 / currency 1070を再現すると、旧`7 → 139（孤立ショップ）`から`7 → 143 → 144（宝箱）→ 146（通常戦闘）`へ変わる。

## v1.13.105: 第2収縮をボスアンカー相似縮小へ修正

- 第2収縮開始円は第一次収縮の最終安置円そのもの。
- ボスnodeは円の中心ではなく、円内の固定アンカーポイント。
- 進捗`p`に対し、`radius = startRadius * (1 - p)`。
- 円中心は`boss + (startCenter - boss) * (1 - p)`。ボスの円内相対位置は縮小中も一定。
- ボスが外周寄りなら、円中心がボス方向へ移動しながら偏心したまま縮む。
- nodeごとに移動円の内外判定を行い、安全→円外へ変わる進捗を二分探索して絶対deadlineへ変換する。
- 実験MAPは現在円中心と半径、boss anchorの円内位置率を表示する。状態chipは「第2収縮中心」から「第2収縮アンカー」へ変更した。
- 座標欠落時だけ従来のbossまでのグラフ距離へフォールバックする。

## v1.13.104: event 18の通貨価値を指数化

- 500・1000の段階式`currencyValueTiers`を廃止した。
- `currencyValueFormula: { minimum: 500, base: 2, interval: 500 }`を使用する。
- 500未満は加算0。500以上は`2^((currency - 500) / 500)`。
- 加算値は500で1、750で約1.414、1000で2、1500で4。
- 1日目の`firstDayValue: 0`は式より先に適用するため、1日目は高通貨でも積極攻略しない。

## v1.13.103: event 18 強敵後20%割引ショップ

- 固定イベント`event 18 / map 3初回実測 / XY 2396,1466 / node 19`を登録した。識別はmap/nodeに依存しない。
- 強敵に勝利すると20%割引ショップへ遷移する。
- `treatAsDanger: true`、`minimumProcessingTurns: 1`として強敵相当のフェーズ評価・期待通貨・収縮滞在時間を適用する。
- 1日目は価値0・空白相当として積極攻略せず、1日目ボス撃破後に強敵価値へ昇格する。
- 所持通貨500以上からspecial価値を指数式で加算する。500未満は割引ショップ分の加算なし。
- 固定辞書登録済みなので、特殊イベント未登録一覧と「今のイベントをコピー」から除外される。

## v1.13.102: 実験MAPで第2収縮を可視化

- 見出しを「収縮・座標シミュレーション」へ変更し、第1・第2収縮で共用する。
- 第2収縮中は`plan.secondShrinkSimulation`を第1収縮モデルより優先する。
- 第2収縮はboss nodeを固定アンカーとする相似縮小として描画する。
- `alreadyMiasmaNodeIds`と現在ターン以前のdeadline nodeを紫の瘴気表示にする。
- node tooltipへ第2収縮の絶対安全期限を表示する。
- 座標欠落時は円を描けないが、グラフ距離由来のnode期限・瘴気表示・指標は維持する。
- 第2収縮のCodex報告schemaは`gbf-second-shrink-simulation-report-v1`。anchor、center、remainTurn、radius、nodeDeadlines、predictedShrinkOrderを含む。

## v1.13.101: event 9を2日目攻略へ変更

- 固定イベント`event 9 / XY 989,648`は強敵4連戦。
- 固定辞書へ`firstDayValue: 0`を追加し、priorityを`day-two-very-high`へ変更した。
- 1日目は目的地として追わない。別の価値マスへ向かう通り道としての通過は妨げない。
- `dayOneBossDefeated: true`以降は、従来どおり通常戦闘4回相当の価値へ昇格する。
- 同一分岐で、1日目は通常戦闘、2日目はevent 9を選ぶ回帰テストを追加した。

## v1.13.100: 導本取得トーストを1効果ずつ縦並び表示

- 1導本効果につき1トーストとする。
- 各トーストは`ID + 効果名`の1行表示とする。
- 1取得バッチに複数効果がある場合は、取得順に上から縦へ並べて同時表示する。
- 各行は7秒後に自動で消す。
- 取得バッチ単位の重複抑止は維持する。

## v1.13.99: 導本取得失敗状態の全タブ共通コピー

- 上部タブ直下へ全タブ共通の「導本未取得をコピー」を追加した。
- 取得トーストが出ない状態で、対象の導本表示が画面に残っている間に押す。
- 既存の`GBF_INSPECT_GUIDEBOOK_STORAGE`を再利用し、URL、DOM、Game.view、constructor/prototype、導本候補パス、導本DOM inventoryを取得する。
- 現在の取得済み導本辞書と、記録が存在する場合のAjax履歴も同梱する。
- クリップボードschemaは`gbf-guidebook-capture-failure-v1`。そのままCodexへ貼り付けられる。
- 診断は最大45秒待機する。完了後は候補パス件数とコピー文字数を上部へ表示する。
- 導本タブ内の「格納場所を調査」は従来どおり同じ詳細診断をJSONファイル保存する。

## v1.13.98: 全タブ共通の導本取得トースト

- サイドパネル最上部へ固定表示される`guidebook-toast-layer`を追加した。
- `recordGuidebookCandidates()`の1回の処理を取得バッチとし、その通信で辞書へ保存・更新した全効果を`captureBatchId / capturedEffects`として更新通知へ添付する。
- 候補、戦闘報酬、イベント、ショップ、効果確認を区別せず、情報を取得できた効果名を通知する。ゲーム内の実所持だけを意味する通知ではない。
- 各効果名はレアリティ・呪い分類に応じて色分けし、1効果ずつ7秒間表示する。
- 同じ取得バッチの重複表示を抑止する。JSONインポートと一覧再読込は取得トーストの対象外。

## v1.13.97: 導本台帳JSONエクスポート／インポート

- 導本タブへ`JSON保存`、`JSON読込`、非表示file inputを追加した。
- エクスポートschemaは既存の`gbf-guidebook-effects-v1`。`exportedAt`と`extensionVersion`を含む。
- 自動生成された完全な空ID枠はファイルから除外する。名称未取得でも獲得履歴やsourceがある実レコードは残す。
- インポートは8MB、10,000件を上限とし、schemaとeffects配列を検証する。
- 既存台帳は置換せず統合する。ID一致を最優先し、ID不明は固定化効果名が一致する既知IDへ寄せる。異なる既知ID同士は名前が同じでも統合しない。
- sourceTypes、captureContexts、observedNames、価格履歴は集合和。firstSeenAtは最古、lastSeenAtは最新。観測・獲得回数は最大値なので同じファイルの再読込で増殖しない。
- ローカルに既知のレアリティ、アイコン、重複可否、呪い分類、価格、所持数などは保持し、importは欠損を補完する。
- 案内文、`[object Object]`、完全な空placeholderは除外する。
- 完了時に追加／統合／除外件数を表示し、一覧を即時再描画する。

## v1.13.96: リザルト案内文除外と残回数導本の統合

- 提供`gbf-guidebook-effects-v1`エクスポートは109件。
- ID不明の「探索画面に戻ることで<br>強力な導本効果を獲得できます。」は効果ではなく、`sephirabook_02_random.jpg`のリザルト操作案内。38回も戦闘報酬として誤観測されていた。
- `isGuidebookInstructionText()`で`探索画面に戻ることで～導本効果を獲得できます`を除外する。報酬抽出時だけでなく`guidebookEffectStorageKey()`でも空キーにするため、既存保存レコードも次回読込時に削除される。
- ID 57の正式な初期表示は「宝箱マスから導本効果を獲得した時、導本効果を追加で1つ獲得する(残り2/2回)」。
- ID不明で`残り0/2回`が1件、`残り1/2回`が4件保存されていた。`(残りn/m回)`を`remaining-count`変数として認識し、初期値を最大値mへ固定してID 57へ統合する。
- 残回数のschemaは`initialValue: 2 / maximumValue: 2 / format: remaining-count`。直近画面値は`lastObservedVariableValues`、全観測文面は`observedNames`へ残す。
- 提供データを修復すると109件から106件、ID未取得は4件から正当なショップ由来1件になる。

## v1.13.95: ボスアンカー型の第2収縮シミュレーション

- 第2収縮は出現したボスを必ずアンカーポイントとして収縮するという実戦仕様を採用した。
- `simulateSecondShrinkArea()`は`miasma.bossNodeId`を優先し、なければ未訪問のnodeType 1からアンカーを特定する。
- 座標取得時はボス座標からの半径、座標欠落時はボスまでのグラフ距離を使用する。
- v1.13.95時点ではボス中心への線形収縮だったが、v1.13.105で第一次最終円のbossアンカー相似縮小へ置き換えた。
- 既に`isShrinking`のノードは現在ターンから瘴気。ボスノードのdeadlineはInfinity。
- 第2収縮経路探索はこのdeadlineを移動・イベント・戦闘の滞在ターンすべてへ適用し、期限切れになる寄り道や接続を除外する。
- 従来どおり第2収縮の瘴気許容量は0%、ボス到着時の目標余力は5ターン。
- planへ`secondShrinkSimulation`を返し、Codexルート報告へstart/current/final center、radius、anchor、nodeDeadlines、predictedShrinkOrderを含める。最適ルート上部には`第2収縮アンカー boss node n`を表示する。

## v1.13.94: 特殊イベント記録済みバッジ

- 最適ルートSVGで、`confirmedFixedSpecialEvent()`が識別できる登録済み特殊イベントに小さな「済」バッジを表示する。
- バッジはノード右上へ12pxずつずらした半径7pxの緑丸で、ノードへ少し重なる。
- 訪問済みノードのopacityに巻き込まれないよう、ノードgroup外の独立overlayとして最後に描画する。
- ツールチップにも`記録済み`を追加する。
- 判定は全MAP共通の固定XYとevent IDを使い、mapごとに変わるnode IDには依存しない。

## v1.13.93: 呪われた導本の赤紫表示

- デメリット効果の呪われた導本は`rarity-cursed`として赤紫の枠・暗い赤紫背景・赤紫IDバッジで表示する。
- 判定は`isCursed: true`、`bookCategory: cursed`、rarity 99、iconCategory 4のいずれか。
- 通常導本の赤銅・白・金クラスより先に呪い判定する。
- 直近選択肢・直近獲得は従来どおり緑のgradientと左線を適用し、呪いの赤紫枠とIDバッジは残す。

## v1.13.92: 状況カウント系導本の固定名・変数分離

- ID 45「所持している呪われた導本効果の数に応じて味方全体の通常攻撃のヒット数増加(＋n/最大＋10)」は、所持状況により本文のnが変化する。
- 辞書名称は初期値0へ固定し、ID 45なら常に`…ヒット数増加(＋0/最大＋10)`とする。異なる現在値で別枠化・上書きしない。
- 対応形式は`(＋n/最大＋10)`、`(n%/最大100%)`などの「現在値／最大値」と、`(n/3回)`など単位付き進捗カウント。
- `effectTemplate`へ`{{value1}}`形式、`effectVariables`へ初期値・最大値・単位・形式、`lastObservedVariableValues`へ直近の画面観測値を保存する。
- 生の変化後文面は`observedNames`へ残す。辞書表示の`name`は0固定なので画面観測値で変化しない。
- 既存保存レコードも`readGuidebookEffects()`で自動移行し、異なる現在値から作られたID不明重複も固定名の同一性で統合する。
- 現段階では変数schemaと直近観測値まで。イベント・アイテム・呪われた導本所持数から`currentValue`を加算するランタイム計算は今後実装する。

## v1.13.91: イベントアイテム所持状態のAjax同期

- 実ログ`gbf-ajax-trace-2026-07-23T11-19-44-134Z.json`は22件。獲得は18番目の`/rest/arcarum3/dungeon/proceed_node_event`。
- 獲得scenarioは`scenario_type: 3 / action_type: 600`で、`dungeon_item_list: [{ dungeon_item_id: 4, num: 1, image_id: "dungeon_item_04" }]`を返す。
- 21番目の`/rest/arcarum3/dungeon/dungeon_item_list`が現在所持品の正本。ID 2は「得体の知れない調合薬」、ID 4は「監獄の鍵」。
- action 600は即時差分として取り込み、同じAjaxの重複受信では個数を増やさない。所持品一覧取得時は一覧全体を正本として置換し、名称・説明・画像を補完する。
- ID 4の有無を`prisonKeyCount`へ自動反映する。これによりXY 1832,350 / event 11は鍵所持時だけ既存のvery-high評価へ上がる。一覧からID 4が消えた場合は0へ戻る。
- ID 2は寄生植物を除去する調合薬だが、event 16用アイテムである根拠はまだない。`event16ItemCount`へは接続しない。
- Ajax候補抽出に`dungeonItem`を追加し、action 600の`dungeon_item_id`と所持品一覧の`item_id`を調査画面で確認できる。

## v1.13.90: 改行差によるID付き・ID不明重複の自動統合

- 提供エクスポートは140枠。正規化比較すると29組が同一内容のID付き・ID不明重複で、統合後は111枠。
- 例はID 122。サーバー名`バトル開始時、敵に被ダメージ無効(30回)(重複不\n可)`とDOM名`...重複不可)`が別キーになっていた。
- 従来の`normalizeGuidebookEffectName()`は改行を空白1文字へ変換するため、単語途中に空白が残った。
- 保存同一性には`guidebookEffectIdentityName()`を使用し、`<br>`と全空白を除去する。表示名自体は変更しない。
- 同じ同一性名に複数の異なるIDがある場合は誤統合防止のため統合しない。IDが0または1種類の場合だけまとめる。
- 読込時に既存保存データも自動修復する。ID付きレコードを主とし、出典、capture context、observedNames、価格履歴、観測回数・時刻、獲得情報を統合する。
- 新規観測時の同名検索にも同じ同一性名を使用し、再発を防ぐ。

## v1.13.89: デメリット導本の即時獲得Ajax

- 実ログ`gbf-ajax-trace-2026-07-23T11-07-19-787Z.json`を解析した。
- URLは`proceed_node_event_unlock_weapon`、scenarioは`action_type: 400`、`candidate_num/select_num: 0`で選択操作なしに付与される。
- 実効果はID 118、名称`毎ターンダメージを受ける`、iconCategory 4、iconType 503、rarity 99、重複可、deckCondition 0、`spacebook_reward_id: 5`。
- action 400のstatus_listは`event_candidate`ではなく`event_reward`として保存し、即時に獲得バッチを付ける。
- 候補ハイライトは付けず、直近獲得の緑ハイライトと「イベント獲得」を表示する。
- action 400かつrarity 99またはiconCategory 4なら`isCursed: true`。
- 導本ページで先にID不明として取得されていても、正規化効果名が同じならID 118へ統合する。

## v1.13.88: 最終日ボス前ショップの割増

- 最終日ボスマスでは戦闘前にショップを利用できる。
- このショップは割増価格で、実測R1導本は通常50コインに対して75コイン。
- `shopPremium`を`shopDiscounted`と独立して保存する。R1では現在価格が通常50を上回れば割増。
- 一覧表示は`75コイン（通常50） / 割増`。
- 通常50、割引38、割増75はすべて`observedShopPrices`へ保持する。
- ボス前ショップを通常ショップノードと識別できるAjax/DOM信号は未採取。現段階では価格状態を保存し、表示場所を価格だけから推測しない。

## v1.13.87: ショップ導本の割引価格

- イベントまたはアイテム所持によってショップ価格が割引される。
- 実測済みはR1導本の通常50コイン、割引後38コイン。
- `shopPrice`は現在表示価格、`shopBasePrice`は通常価格、`shopDiscounted`は割引判定として別々に保存する。
- 同じ効果で観測した価格は`observedShopPrices`へ重複なく蓄積する。
- R1かつ現在価格が50未満なら割引。一覧は`38コイン（通常50） / 割引`形式。
- R2・R3の通常価格・割引価格は未実測なので辞書化しない。新データ取得までは現在価格だけを扱う。
- ショップ購入・売切変化の獲得判定は価格固定値に依存していないため変更なし。

## v1.13.86: 導本ハイライト失効と非破壊マージ

- `lastCandidateBatchId`と`lastAcquisitionBatchId`は履歴として保存するが、表示ハイライトは各観測時刻から60秒以内だけ有効。
- 失効時刻にサイドパネル側のtimerで再描画し、背景色・ラベル・ステータスを自動解除する。
- 同名または同IDの効果を再観測する際、rarity、iconCategory/type、isDuplicationPossible、deckCondition、target、画像、cursed区分は非`null`・非空の新値だけで更新する。
- 戦闘報酬や効果確認のように一部メタ情報を持たない経路は、既知のランク・重複可否を消さない。出典`sourceTypes`は従来どおり和集合で保持する。
- 導本行はID＋名称の下にメタ情報を全幅で折り返して表示し、長い出典ラベルが名称やR/重複表示を圧迫しない。
- すでに過去バージョンで失われたメタ情報は、同じ候補を再表示するとサーバー値で復元される。

## v1.13.85: 呪われた導本の確認ページ取得

- 診断`gbf-guidebook-storage-2026-07-23T10-16-28-538Z.json`は`#arcarum3/book`の呪われた導本タブ表示中。
- 通常効果と同じ`LI`構造で、選択タブ画像は`btn_cursed_on.png`。効果本文は`バトル開始時、敵に被ダメージ無効(30回)(重複不可)`、アイコンは`book_effect_501.png`だった。
- 導本ページDOM取得時に`unique/rare/normal/cursed`の選択中タブを判定し、`bookCategory`と`isCursed`を保存する。
- アイコンファイル名から`iconType`を保存する。この501は効果IDではなくアイコン種別なので、`status_id`は推測せずID不明として保持する。
- 一覧メタ情報へ「呪われた導本」を表示する。
- DOM収集値の明示的`null`を0へ変換しないよう、rarity/icon/count/shop系のoptional number正規化を修正した。
- イベント獲得直後の診断`10-15-13`には効果情報が残っていなかったため、効果IDおよび獲得バッチの確定には選択時Ajaxが引き続き必要。

## v1.13.84: 導本レアリティ色と一時現在地・宝箱消化の補正

- 導本効果の枠線とIDバッジは`rarity`（ショップは`shopBookGrade`を予備値）により、1=赤銅、2=白、3以上=金で表示する。未判明は灰色。
- 直近選択肢と直近獲得はレアリティの金色と競合しない緑系ハイライトへ変更した。
- 浮遊城本体の孤立ノード`type 10 / specialType 4 / XY 346,292`は演出上の実現在地として`actualCurrentNodeId`へ保持する。
- ルート計算用`currentNodeId`は移動前の浮遊城転移マスを`floatingCastleReturnNodeId`として維持する。孤立ノード1件だけの一時`node_list`では既存MAPを置換しない。
- `move_node`応答に`after_current_node_id/current_node_id`がない場合、requestの`node_id`を現在地として補完する。
- ノード離脱時は更新前のnode種別も参照して通常戦闘・通常イベント・宝箱・回復を消化済みにする。宝箱が応答側ですでに空白へ再分類されても`originalType: 6`を残し、元の宝箱へ戻る提案を防ぐ。

## v1.13.83: ID 1〜100台帳と実獲得ハイライト

- 添付エクスポートは55件で、正常なID付き50件、ID不明4件、無効な`[object Object]`1件だった。
- 既存保存データを上書きせず、`guidebookEffectsWithIdPlaceholders()`で返却・表示時だけID 1〜100の欠番を空枠補完する。
- ID 101以降は実際に観測されたレコードだけ追加する。ID不明の名称レコードも保持する。
- 表示状態は「すべて」「未獲得（空枠）」「獲得済み（ID・名称採取済み）」「ID不明」。
- 候補表示は従来どおり`lastCandidateBatchId`で黄色表示する。
- 実獲得は`lastAcquisitionBatchId/lastAcquiredAt/lastAcquisitionSource`で別管理し、直近バッチを青緑表示する。
- 戦闘リザルトは`battle_reward`、イベントは実ログで確認した`proceed_node_event_spacebook_status_add`のrequest `status_ids`、ショップは購入系URLのstatus IDまたは売切変化、効果確認は既知countの増加を獲得として扱う。
- ショップ・導本確認ページにいる間だけAjax後0/250/750msにDOMを再取得し、売切・count変化を自動検出する。
- `[object Object]`レコードを削除し、正常レコードの`observedNames`に混入した同文字列も読み込み時に清掃する。

## v1.13.82: 生リザルトdetail配列と表示用配列の統合

- 診断`gbf-guidebook-storage-2026-07-23T09-45-22-574Z.json`で、表示用配列は`window.Game.view.arcarum3RewardList`にあり、導本効果は`comment`文字列として正常に保持されていた。
- 同じリザルトの生データは`content_model.attributes.option.result_data.arcarum3.reward_list`にあり、`reward_type: 4`、`image: sephirabook_02`、`detail: [{status_id: 33, icon_type: 10, name: 通常攻撃のヒット数増加(＋1)}]`形式だった。
- 旧手動探索は生データの`detail`配列を単一値として扱い、`[object Object]`へ変換していた。
- 自動監視はconstructor側だけを参照しており、実際の`view.arcarum3RewardList`を見落としていた。
- 表示用配列、constructor側、生`reward_list`の3経路を監視し、同じ効果は送信前に統合する。
- 生`detail`は効果ごとに展開し、`status_id`と`icon_type`を保存する。無効な`[object Object]`レコードは読み込み時に除外する。

## v1.13.81: 浮遊城など別形式イベントの導本取得

- Ajax履歴の「記録開始」は履歴・OPFS保存だけを制御し、page-hookのAjaxフックと導本保存は常時動作している。
- 旧`guidebookCandidatesFromPayload()`はURLを`proceed_node_event`に限定し、直下`action_scenario_list`の`action_type: 401`だけを見ていた。
- arcarum3配下の全Ajaxレスポンスを深度24まで再帰探索し、任意階層の`status_list/statusList`を検査する。
- `status_id/statusId`、nameに加え、rarity・iconCategory・iconType・重複可否・deckConditionのうち2項目以上があれば導本固有スキーマと判定する。
- snake_case/camelCaseの両方を正規化する。従来の`action_type: 401`は明示的導本scenarioとしてそのまま対応する。
- `Game.view`の手動探索にも同じ汎用`status_list`判定を追加した。
- 別URL・深い入れ子の浮遊城fixtureを追加し、メタ情報がiconTypeだけの通常戦闘バフを除外することを固定した。

## v1.13.80: リザルト低レアリティ導本の取得漏れ修正

- 取得漏れ報告は「渾身」「背水」「クリティカル確率UP(30%)」。`自属性攻撃UP(20%)`は取得できていた。
- 旧実装は`rewardType === 4 && commentが非空`を必須にし、効果名としても`comment`しか使用していなかった。
- 効果本文を`comment → name → effectName/effect_name → description → detail → text`の最初の非空値から取得する。
- `rewardType: 4`に加え、画像またはアイコンURLが`sephirabook`/`book_effect`なら導本として扱う。
- リザルトArray署名へname、effectName、description/detail/text、`bookEffectIcon`の正規綴り、countを追加した。
- `comment`空＋`name: 渾身`、`description: 背水`、`effectName: クリティカル確率UP(30%)`のfixtureを追加した。通常アイテム画像は除外される。
- 実リザルトの生オブジェクトは既存ログに残っていなかったため、再発時はリザルト表示中に「格納場所を調査」を実行してconstructorの`arcarum3RewardList`を採取する。

## v1.13.79: 呪われた導本の構造診断を拡張

- v1.13.76の診断ファイルは実DOM約631万文字に対し、`page.html`が先頭100万文字で切れていた。
- 呪われた導本の実行部分は保存範囲外で、既存ファイルだけでは通常導本との差を確定できない。
- 診断JSONへ`guidebookDomInventory`を追加し、`#root li`最大5000行の本文・画像・class・8階層の祖先・各行HTMLを個別保存する。
- 導本関連画像も最大5000件、画像URL・親本文・親HTML付きで別途索引化する。
- 導本確認ページの取得selectorを`book_effect_`ファイル名固定から`/arcarum3/assets/icon_book_effect/`フォルダ全体へ拡張した。
- 呪われた導本タブを開いた状態で新しい「格納場所を調査」を実行し、`guidebookDomInventory`から専用class・画像・行構造を確定する。

## v1.13.78: ショップページの導本DOM取得

- 診断ファイル`gbf-guidebook-storage-2026-07-23T09-14-13-098Z.json`は`#arcarum3/dungeon_shop`。
- ショップ商品も`Game.view`の専用配列ではなくLit描画DOMに存在する。
- `#js-prt-dungeon-shop-content-list`内の`book_effect_*.png`を持つ商品行だけを取得する。
- 効果本文、`必要セフィラコイン`の価格、`badge_soldout.png`の有無、`base_book_n.png`の台紙種別を保存する。
- `icon_book_remove.png`の導本削除サービスは効果ではなく、`book_effect_*.png`を持たないため対象外。
- `shop_page`として名称統合し、既存の導本確認・イベント・戦闘報酬と同名なら一件へまとめる。
- UIには「ショップ / nコイン / 売切」を表示する。別画面で同名効果を再取得してもショップ固有情報を消さない。

## v1.13.77: 導本効果確認ページのDOM取得

- 診断ファイル`gbf-guidebook-storage-2026-07-23T09-09-32-284Z.json`は`#arcarum3/book`の導本効果確認ページ。
- 効果一覧は`Game.view`のデータ配列ではなく、Litで描画されたDOMに存在する。
- `Game.view.rawResponseData.display_list`はダマスカス骸晶等の通常アイテム所持一覧であり、導本ではない。
- `#arcarum3/book`では`img[src*="/arcarum3/assets/icon_book_effect/book_effect_"]`を起点に、同じ行の効果本文・`×n`所持数を取得する。
- IDはDOMに存在しないため名称キーで保存し、画像URLを`bookEffectIcon`、所持数を`count`として保持する。
- 汎用Game.view判定から「ID＋rarityだけ」の条件を外し、通常アイテムを導本効果として誤取得しないようにした。
- ショップ画面は別形式のため、ショップ表示中の診断JSONを解析後に専用取得を追加する。

## v1.13.76: 導本格納場所の診断JSON

- 導本リストへ「格納場所を調査」ボタンを追加した。実行すると`gbf-guidebook-storage-*.json`をダウンロードする。
- 診断対象は`window.Game.view`（深度12）、`Game.view.constructor`のown property、constructor prototype、現在ページDOM（最大100万文字）。
- 導本関連キー、効果らしい文字列、`name/comment/image/rarity/status_id/effect_id`等の組み合わせを最大2000件の`matches`へパス付きで索引化する。
- 実オブジェクト探索は最大深度14・最大30000 object。循環参照、getter、DOMノードを安全に処理する。
- 大きい診断データを想定し、`inspect_guidebook_storage`要求だけcontent scriptの応答待ちを45秒にした。
- ショップ表示中と導本効果確認表示中に各1回保存し、2ファイルをCodexへ渡して実際の格納形式を比較する。

## v1.13.75: ショップ・導本効果確認ページの手動取得

- 導本リストの「現在画面から取得」で、イベント選択肢と戦闘リザルトに加えてショップページ・導本効果確認ページを探索する。
- `Game.view`探索中に`rewardType: 4`を全階層から回収する。導本固有画像、導本・ショップ系の格納経路、IDと導本候補メタ情報の組み合わせも判定材料にする。
- `action_type: 401`配下と`rewardType: 4`は一般探索から除外し、ショップ・効果確認として二重計上しない。
- 取得した項目は`shop_page`または`effect_confirmation`を収集元に保持し、UIへ「ショップ」「効果確認」と表示する。
- ID不明でも効果名で保存し、既存の名称統合・後日のID統合を利用する。イベント選択肢ではないため直近候補ハイライトは更新しない。
- UIの取得結果は「選択肢 / ショップ・効果確認 / 戦闘報酬」の件数を分けて表示する。

## v1.13.74: 導本の現在画面からの手動取得

- 導本リスト見出しへ「現在画面から取得」ボタンを追加した。
- `GBF_CAPTURE_GUIDEBOOK_EFFECTS`をsidepanel→background→content→page-hookへ中継する。content script不在時は既存`requestPageCapture()`が再注入する。
- page-hookは現在の`Game.view`を最大深度10・最大10000 objectまで探索し、`action_type: 401 / status_list`を回収する。DOM、循環参照、getterは辿らない。
- `constructor.arcarum3RewardList`の`rewardType: 4`も同時に直接取得する。
- backgroundは`guidebook_manual_capture`として既存の自動収集処理へ投入するため、ID統合、効果名統合、直近選択肢batch、永続保存は共通。
- UIへ「選択肢 n件 / 戦闘報酬 n件」を表示する。両方0件なら現在のGame.viewに情報が残っていないと表示する。
- Game.viewから消えた過去情報の復元機能ではないため、選択肢・リザルトが表示されている間に使用する。

## v1.13.73: XY 346,292の浮遊城本体を評価対象外へ

- `nodeType 10 / specialType 4 / XY 346,292`の接続を持たないノードは浮遊城本体。
- 導本獲得演出に使う座標であり、実際の現在地は浮遊城への転移マスに残るため、移動先として扱わない。
- `isFloatingCastleBodyNode()`を追加し、ルート用graphから完全除外した。価値、空白、通過点、終点、退避先、期限付き候補のいずれにもならない。
- 専用表示は城アイコン＋「浮遊城本体」。specialType 5/6/7の転移マスとは分離する。
- backgroundの現在特殊イベント観測と、sidepanelの保存済み・新規未登録イベント候補からも除外する。
- specialType 4だけでは判定せずXYも要求するため、将来別座標に同じ種別が出ても誤除外しない。

## v1.13.72: 第一次収縮フォールバックの強敵経由を抑制

- 実報告はmap 4、1日目T22、第一次収縮残り7ターン、現在node 73。旧案は`73→78（空白）→77（超強敵）→13（空白）`だった。
- 原因は5ターン余裕を満たす価値候補がなくなった後、初期退避経路が移動・処理時間だけで最短を選び、1日目の未訪問強敵系を中間通過できたこと。
- 通常退避に加え、1日目未訪問強敵系を除外した退避を計算する。非強敵経路が収縮完了までに恒久安置へ届くならそちらを優先する。
- 完全報告を再計算し、`73→2→74（イベント）→70→69→27（イベント）`へ変更された。恒久安置node 27への到着は処理見積もり込み6ターンで、node 27の処理完了は7ターン。どちらも収縮残り7ターン以内に入る。
- 非強敵経路が`remainTurn`を超える場合は、ルートを消さず従来の強敵経由最短路を最後の手段として残す。
- 縮約fixtureで、残り7ターン時は非強敵経路、残り5ターン時は不可避な強敵経路へ切り替わることを固定した。

## v1.13.71: 直近の導本選択肢をハイライト

- `action_type: 401`を受信するたびに、時刻＋連番の`lastCandidateBatchId`をその`status_list`全件へ保存する。
- 最新batchの行だけを黄色の枠・左帯・背景で強調し、「直近の選択肢」と表示する。
- 既知の効果が再表示された場合も最新batchへ更新する。次の選択肢を観測するまでハイライトを維持する。
- 戦闘リザルト報酬は選択肢batchを作らず、同名レコードへ統合しても既存の直近選択肢情報を保持する。

## v1.13.70: 戦闘リザルト導本の取得漏れ防止

- 旧実装はAjax後0/100/300/700msだけ`arcarum3RewardList`を確認していたため、描画が700msより遅い場合に取得漏れがあった。
- 250ms間隔の常時監視へ変更し、Ajaxの有無や演出時間に依存しない。
- `pageshow`と`visibilitychange`からの表示復帰時も即時確認する。
- 同一Array参照・同一内容は送信しない。Array内容が同一参照のまま後から書き換えられた場合はsignature差で検出する。
- 画面遷移中の一時的な読み取り例外は次回監視で再試行する。

## v1.13.69: 戦闘リザルト導本の収集

- `window.Game.view.constructor.arcarum3RewardList`から`rewardType: 4`を抽出する。
- 戦闘リザルト表示後に配列更新を短時間再確認し、`comment`を導本効果本文として保存する。
- リザルト報酬にはIDがないため、正規化した効果本文をキーにして「ID未取得」「戦闘報酬」と表示する。
- 同じ効果名のイベント候補が既にある場合はそのIDへ統合する。戦闘報酬が先でイベント候補が後の場合も、後からID付きレコードへ移行する。
- 画像URL、導本効果アイコンURL、詳細表示可否、収集元を保存・JSONコピー対象に含める。

## v1.13.68: 導本名を効果本文として使用

- Ajaxの`status_list[].name`自体が導本の効果本文であることを確認した。
- 導本リストの手入力欄、保存ボタン、説明編集メッセージを削除した。
- `@@`は画面表示とJSONコピーで改行へ正規化する。
- 候補全件の自動収集、ID単位の永続保存、観測回数・時刻、検索、一覧コピーは維持する。

## v1.13.67: hit予測の削除と導本効果リスト

- 保留中だったhit予測の計算、通常攻撃直前スナップショット、奥義ゲージ・バフ取得、学習履歴、表示タブを削除した。旧v3/v4学習履歴は更新時にストレージから削除する。
- 旧hit予測タブを「導本リスト」へ置き換えた。
- `/rest/arcarum3/dungeon/proceed_node_event`の`action_type: 401`に含まれる`status_list`全件をID単位で保存する。選択・獲得結果ではなく候補レスポンスを使うため、獲得しなかった候補も記録できる。
- ID、名称、レアリティ、重複可否、選択数、初回・最終観測時刻、観測回数を表示する。各効果へ説明を入力して永続保存でき、絞り込み・再読込・JSONコピーに対応する。
- キャラクター別表示のフロント判定は、軽量な`frontFormation`取得として維持する。

## v1.13.66: 期限順序の修正と推奨ルート報告

- 第一次収縮中、期限T27の行き止まりイベントへ先に寄って折り返し、期限T21の戦闘を後回しにする提示を再現して修正した。
- `expiringValue`は、移動と初回処理を含む`departureTurn`がnodeの安全期限より前の場合だけ加算する。期限後の回収を同じ「安置外回収価値」として扱わない。
- 同じ期限付き価値を回収できる候補では、各nodeの`elapsedTurns / (deadline - totalTurn)`の最大値を小さくする。これにより締切の早いnodeを後回しにする経路を抑制する。期限後に到達する期限付き価値は`lateExpiringValue`として明示的に劣後する。
- `plan.routeTimeline`へ各手順の`arrivalTurn`、`processingTurns`、`departureTurn`、`deadline`、`completesBeforeDeadline`、瘴気回数・ダメージを追加した。UIにも到着・処理後・期限を表示する。
- 推奨手順見出しに「Codex報告をコピー」を追加した。報告は正規化状態、全nodeの物理接続と探索用接続、全期限、採択path、手順timeline、期限超過、node重複、`A→B→A`折返しを含む。

## v1.13.65: 第一次収縮シミュレーションのCodex報告コピー

- 「第一次収縮・座標シミュレーション」右上に「Codex報告をコピー」を追加した。
- コピー内容は、map/pattern/step、現在地・ターン、miasma状態、登録モデルと計算中心・半径、全ノードのID/XY/type/接続/訪問状態、取得データ上の`isShrinking`、シミュレート瘴気判定を含む。
- `gameStateOnlyShrinkingNodeIds`と`simulationOnlyMiasmaNodeIds`を自動計算する。ただし`isShrinking`は画面を目視した実測値とは限らないため、報告文先頭に「実際の円内」「実際の円外」「境界がずれている箇所」の手動追記欄を置いた。
- patternモデル未登録でも全ノード座標を取得できていればコピー可能とした。モデル欠落そのものの調査にも同じ報告を使用できる。

## v1.13.64: 浮遊城を特殊イベント候補から分離

- 浮遊城への転移（specialType 5/6/7）と浮遊城の研究者（specialType 8）は内容判明済みで、転移出現座標もランダムなため、固定座標を採取する一般特殊イベントとは別カテゴリとする。
- `isFloatingCastleSpecialEventRecord()`を追加し、初期node一覧からの候補作成、現在地からの候補作成、Ajax進入履歴、未登録一覧、表示件数、現在イベントコピーから除外した。
- backgroundは浮遊城関連へ進入しても`specialEventObservation`を作成しない。直前の一般特殊イベントが誤って現在イベントとして残らないよう、浮遊城進入時は観測値をnullへクリアする。
- `route-special-event-observations`に過去保存されたspecialType 5/6/7/8は、次回収集時に読み飛ばして保存し直すため自動削除される。
- これは特殊イベント調査候補だけの分離である。地図上の浮遊城専用アイコン、研究者の通行禁止、通常時の転移1/3期待値、第一次収縮残り10ターン以下・第二収縮中のランダム転送回避は維持する。

## v1.13.63: 瘴気許容を第一次収縮だけに限定

- 瘴気通過を許容するのは、miasmaがactiveかつ`level === 1 / status === 1`の第一次収縮中だけとする。
- 第一次収縮完了後（level 1 / status 2）と第二収縮（level 2またはstatus 3以降）は`miasmaHealthBudgetPercent()`を常に0とする。HP85%以上・絶対HP60,000以上を満たしていても瘴気へ入らない。
- 最短経路、価値探索、ボス寄り道、退避探索、保守的フォールバックはすべて同じ許容量関数を使用するため、瘴気ノードを1回でも含む候補は全経路種別で除外される。
- 第二収縮で安全経路が存在しない場合、危険経路へ緩和せず「安全な移動先なし」とする。以前追加した「どの状況でも案内を出す」フォールバックより、今回の瘴気全面禁止を優先する。
- 第二収縮のボス到着残り5ターン目標は、瘴気を通らない経路同士の寄り道予算として継続する。
- UIの状態欄は`瘴気通過`へ統一し、第一次収縮中は`許容 n%`、その他の収縮中は`禁止`、未発生時は`対象外`と表示する。従来の`確保HP 85%以上`チップは削除した。

## v1.13.62: 1日目君臨者の退避終点化とフェーズ判定を修正

- 実機で1日目22ターン目の第一次収縮中に「空白→君臨者」を推奨した。22ターンなので従来の50ターン境界による2日目誤判定は直接原因ではなかった。
- 原因は`estimateCoreNodeIds()`が「期限Infinity＝恒久安置」とだけ判定し、1日目の未訪問強敵・君臨者・超強敵も退避終点へ含めていたこと。
- 1日目の未訪問強敵系を恒久安置候補から除外した。撤退済み・訪問済みなら空白化済みとして候補に戻せる。
- 1日目強敵系を全面通行禁止にはしていない。以前の要件どおり空白1個相当を維持し、その先に価値または安全な終点がある場合の中間通過は可能。ただし強敵系そのものを退避・推奨経路の終点にしない。
- 別件として、`totalTurn >= 50/59`だけで2日目・2日目収縮へ進める暫定判定も廃止した。`dayOneBossDefeated`の実測フラグと現在のmiasma状態で判定し、1日目が長引いても2日目価値へ誤昇格しない。
- 特殊イベント出現時期の記録もターン50ではなく`dayOneBossDefeated`だけを使用する。
- 最適ルート状態欄へ`フェーズ: 1日目 / 2日目前半 / 2日目・収縮中`を追加した。

## v1.13.61: 複数ターン洞窟探索event 16

- `event 16`を全MAP共通の固定座標`1597,1062`で辞書登録した。最初の実測はmap 1 / node 17だが、map/node IDは識別条件に含めない。
- 複数ターンを使って洞窟を探索し、イベントアイテム未所持時は1マス上で複数回戦闘できるため移動効率が高い。通常イベントより高い`priority: high / value: 1`とする。
- イベントアイテム所持時は使用以外の選択肢がなく、確率で戦闘を回避する代わりに多めの通貨を得る。`withItemOutcome`と`withoutItemOutcome`を辞書へ保持する。
- 戦闘回数、通貨量、最終的な総ターンが未確定なので、アイテム所持による価値差はまだ数値化しない。`event16ItemCount`状態を受け付ける構造だけ用意したが、実レスポンスの取得元フィールドは未特定。
- 「複数ターン」から確定できる下限として`minimumProcessingTurns: 2`を設定した。経路時間と瘴気滞在時間には移動1ターンとは別に最低2ターンを加算し、実際の`elapsedTurn`更新後に再探索する。
- 登録済み固定イベントとして、特殊イベント座標記録の未登録一覧から除外する。

## v1.13.60: 浮遊城のランダム転送リスク

- 浮遊城への転移（specialType 5/6/7）は3箇所中1箇所が最高レアリティ導本の正解で、不正解2箇所は無報酬ではなく完全にランダムなエリアへ転送される。
- 通常時は従来どおり正解確率1/3の低い期待報酬として扱う。
- 第一次収縮中かつ`remainTurn <= 10`、または第二収縮中（`level >= 2`または`status >= 3`）は、ランダム転送による退避不能・ボス到着遅延リスクが上回るため、転移マスを経路グラフから除外する。目的地だけでなく、最短経路の通過点にも使用しない。
- 残り11ターンでは通常評価を維持し、10ターンで除外へ切り替える境界fixtureを追加した。
- すでに危険フェーズの転移マス上にいる場合は現在地ノードだけをグラフへ残す。転送を拒否したまま隣接する通常マップへ離脱する経路を算出できる。

## v1.13.59: 条件付き固定特殊イベントevent 11

- `event 11`を全MAP共通の固定座標`1832,350`で辞書登録した。最初の実測はmap 1 / node 12だが、map/node IDは識別条件に含めない。
- 監獄の鍵を所持している場合、無条件で通貨500相当または最高レアリティの導本効果を得られる。`currencyEquivalent: 500`、条件成立時`very-high / value 2`として扱う。
- 鍵未所持時は確率判定または強敵戦闘が必要で利益が薄いため、追加special価値0の通常イベント相当とする。鍵所持不明時も誤った高評価を避けるため同じ評価にする。
- `normalizeState()`へ`prisonKeyCount`を追加し、`prisonKeyCount` / `prison_key_count` / `jailKeyCount` / `jail_key_count`と`hasPrisonKey`系の入力を受けられるようにした。ただし実レスポンス上の取得元フィールドは未特定であり、backgroundからの自動供給はまだない。
- 固定イベント辞書登録済みのため、特殊イベント座標記録の未登録イベント一覧には表示しない。

## v1.13.58: 固定特殊イベントevent 9

- `event 9`を全MAP共通の固定座標`989,648`で辞書登録した。最初の実測はmap 1 / node 10だが、map/node IDは識別条件に含めない。
- イベント内でターンを進めず4回の戦闘を行え、良質なドロップが多い。`battleEquivalent: 4`、`processingTurnOptions: [0]`として保持する。
- 既存評価では通常戦闘1回がactivity 2点なので、event 9はactivity 2点とspecial 6点の合計8点となる`value: 2`を使用し、通常戦闘4回相当として扱う。
- 一般の未分類特殊イベントと異なり1日目でも候補に含める。HP・瘴気・退避期限・ボス到着余裕などのhard constraintは引き続き優先する。
- 登録済み固定イベントとして、特殊イベント座標記録の未登録イベント一覧・件数・現在イベントコピー対象から除外する。

## v1.13.57: 第二収縮のボス到着余裕5ターン

- 各日の第二収縮のボス経路は、ボス到着時点で`remainTurn`を5ターン以上残すことを目標にする。
- 寄り道予算を従来の`remainTurn - 1`から`remainTurn - 5`へ変更した。経路上の移動に加え、未処理の戦闘・イベント滞在ターンも同じ予算から差し引く。
- 5ターンを残せない寄り道は破棄し、安全な最短ボス経路へ切り替える。
- 現在地がすでに遅く、最短経路でも5ターン未満になる場合はルートを非表示にしない。最短経路を提示したうえで「最短経路でもボス到着時の収縮余力はnターン」と警告する。
- 最適ルートの状態欄に`ボス到着余力 nT / 目標5T`を表示する。

## v1.13.56: 推奨経路の保守的フォールバック

- 通常の価値探索・ボス探索が現在地だけを返した場合、別のフォールバック層で全未回収価値への到達可能性を調べる。
- フォールバックは、第一次収縮の退避、既知の安全制約内で到達できる価値マス、安全圏への退避、安全な隣接移動の順で候補を返す。座標シミュレーションやpatternモデルが欠けていても、既知の`is_shrinking`、収縮段階、HP制約だけで成立する。
- 価値マスまで空白・処理済みマスが連続し、通常beam searchの探索長を超える場合も最短経路を提示する。
- 1日目の強敵・君臨者・超強敵と、期待通貨500未満のショップはフォールバックの隣接候補にも含めない。
- 安全制約を満たす隣接先さえない場合は`blocked`とし、推奨欄を無言で空にせず「安全な移動先なし」を表示する。

## v1.13.55: 第一次収縮シミュレーションを実測patternモデルへ復元

- CreateJS上の収縮円画像について、表示倍率またはtransformed boundsを取得できないため、v1.13.46の実表示半径必須方式を一旦廃止した。
- `FIRST_SHRINK_PATTERN_MODELS`を復元し、完全キー`4:3:1`とMAP共通キー`*:1:1`を登録した。表示倍率、`circleRadius`、`limitCircle.position`はシミュレーション成立条件にしない。
- `4:3:1`はOPFSログ`2026-07-22T14-38-44-345Z`の実測式を使用する。最終中心`867.9,1000.5`、半径`1655.9043615 - 9.89789339 × step`。
- `*:1:1`はmap 1とmap 3で共有確認済みのモデルを使用する。最終中心`843,629`、途中半径`1600 - 9.25 × step`、step 100またはlevel 1/status 2の完了時半径650。
- 完全キーをMAP共通キーより優先する。どちらにも一致しないpatternは全ノードだけを表示し、「実測モデル未登録」と案内する。既知モデルを未登録patternへ流用しない。
- UIの計算元表示を`OPFS実測モデル <modelKey>`へ戻し、「実表示倍率・boundsを取得してください」という警告を削除した。

## v1.13.54: 登録済み特殊イベントを観測表示から除外

- 「特殊イベント座標記録」は未登録イベントの採取・報告用表示とする。
- `route-planner.js`の固定イベント辞書に座標・event IDが一致する記録は、一覧、表示件数、現在イベントのコピーボタン対象から除外する。event 10とevent 13が対象になることをfixtureで確認済み。
- `chrome.storage.local`内の観測履歴は削除しない。map/pattern別の出現履歴を保持したまま、描画時だけ未登録記録へ絞り込む。
- 登録済みイベントへ進入中でもコピーボタンは無効になり、表示は「未登録イベント進入待ち」となる。

## v1.13.53: ボス前HP85%・絶対HP60,000制約

- 第二収縮（`level >= 2`または`status >= 3`）の瘴気許容量は、全生存メンバーについて予定ダメージ後も最大HPの85%以上かつ絶対HP60,000以上を残す範囲に限定する。
- 必要HPはキャラクターごとに`max(ceil(maxHp * 0.85), 60000)`で算出し、現在HPとの差を瘴気ダメージ許容量へ変換する。最も余裕のない生存メンバーを全経路の上限とする。
- 現在HPが60,000未満なら許容量0。現在HPが60,000以上でも、1回の瘴気で60,000未満または85%未満になる場合は瘴気ノードを除外する。
- 最大HP100,000・満タンなら15%を1回許可して85,000を残す。最大HP70,000・満タンなら15%後が59,500となるため進入を禁止する境界fixtureを追加した。

## v1.13.52: 後半の価値密度評価と固定event 13

- 退避経路やボス経路が往路と重なる場合、同じ戦闘・イベントへ処理ターンを再加算していた。計画内ですでに処理済みのノードは、再訪時に移動1ターンだけを数えるよう修正した。
- 1日目ボス撃破後の後半フェーズでは、空白数と折り返しを価値より常に先に比較する方式をやめ、フェーズ別戦闘価値と回収価値密度を先に比較する。同程度の価値なら従来どおり空白・折り返し・瘴気を避ける。
- これにより、短い価値枝を折り返す案より、空白が連続する周回へ誘導する挙動を抑制する。1日目前半の空白最優先と、HP・収縮期限・5ターン退避余裕のhard constraintは維持する。
- 固定特殊イベント`event 13`を座標`2052,617`で登録した。最初の実測はmap 4 / node 13だが、辞書照合はmap/nodeに依存しない。
- 選択肢1は3ターン経過で600通貨、選択肢2は導本効果2つと新しい導本2つの交換。どちらも高価値なので辞書値を`very-high / value 2`とし、通常イベントより優先する。
- ルート成立判定はターン消費が明記されていない選択肢2を利用可能な高価値代替として扱う。選択肢1を選んだ場合はAjaxの`total_turn +3`と通貨増加を取り込み、直後に残り経路を再探索する。

## v1.13.51: 戦闘・イベント滞在時間と5ターン退避余裕

- 通常戦闘、強敵、超強敵、君臨者、通常イベント、狂信者、邪教祖は、移動1ターンとは別に処理最低1ターンを見込む。
- 処理中にノードが瘴気内なら、その滞在ターンにも段階別瘴気ダメージを累積する。推奨経路、恒久安置への退避経路、第二収縮のボス寄り道探索で同じ時間計算を使用する。
- 第一次収縮中の価値回収は、`totalTurn + remainTurn - 5`までに恒久安置へ到達できる候補だけ許可する。余裕を確保できる価値候補がなければ、現在地から最短退避経路を返す。
- 実際の戦闘が2ターン以上かかった場合は、Ajaxの最新`total_turn`で再探索する。最低1ターン見積もりと5ターン余裕を組み合わせ、長期滞在の不確実性を吸収する。
- 模擬fixtureで、イベントの移動＋処理を2ターン・瘴気2回として数えること、遅い戦闘寄り道を除外して5ターン余裕を残すこと、第二収縮の寄り道予算にも処理時間を含めることを固定した。

## v1.13.50: 強敵系価値の2日目段階評価

- 強敵・君臨者・超強敵に共通のフェーズ価値を追加した。
- 1日目は従来どおり価値0・空白1個相当で、撤退前提のため期待通貨も0。
- 1日目ボス後から2日目第一収縮前（実測境界T50～58）は価値1。通常戦闘・イベントの価値2より低いが、空白ではない。討伐可能として期待通貨60を加える。
- 2日目第一収縮開始後（miasma activeまたはT59以降）は価値3。通常戦闘・イベントの価値2より高くし、2日目後半もこの高価値を維持する。
- 通常活動と強敵系の両方を含む候補同士では、2日目前半は通常活動、収縮開始後は強敵系というカテゴリ順位を効率計算より先に適用する。
- このカテゴリ比較は双方が戦闘・イベント系価値を持つ場合だけ行い、遠い通常イベント寄り道が短い宝箱ルートを再び上回らないようにした。
- 撤退済みとして`isPassedDanger`になった強敵系は、2日目でも空白相当のまま再評価しない。

## v1.13.49: 1日目強敵系とショップ通貨評価の修正

- 1日目（現行フェーズ境界では`totalTurn < 50`）の強敵・君臨者・超強敵は、戦闘しても必ず撤退する無価値マスとして空白1個相当に変更した。通常のdangerとemptyを重複加算せず、空白と同じ評価値にする。
- 到着時期待通貨が500未満のショップも空白1個相当とする。価値を回収しない空白・1日目強敵系・資金不足ショップだけの経路はfallback推奨にせず、現在地待機とする。
- 通常戦闘だけが期待通貨を60増やす。1日目強敵系から撤退しても通貨は増えないため、資金0の強敵→ショップ経路は購入可能にならない。
- ショップは到着直前の推定通貨で判定し、499以下は無価値、500以上で購入価値を5点相当として有効化する。これにより戦闘後ちょうど500へ達する経路はショップまで表示し、499では戦闘prefixで止める。
- 期待通貨1000未満ではショップ1個まで、1000以上の場合だけ2個以上を許可する既存条件は維持する。

## v1.13.48: 第一次収縮開始時の安置外価値回収

- `level:1 / status:1`の第一次収縮中は、最終安置の外側にあり、まだ瘴気化していない未訪問価値ノードを`future-expiring value`として分類する。
- 実表示円半径`circleRadius`がある場合は、最終中心から半径外のノードだけへ期限を設定する。半径未取得時だけ従来の外側62%推定へフォールバックする。
- future-expiring valueの累積価値と最初の到達手数を、安置内の宝箱・連続戦闘・イベントより前に比較する。円が表示された時点で安置外方向へ切り替わる。
- 安置外価値への最短距離をbeam searchの誘導値に追加し、大規模マップで安置内候補だけがbeamを占有しないようにする。
- HP基準の瘴気生存可能性、第一次収縮完了までの恒久安置への退避可能性はhard constraintのまま維持する。
- このモードは画面に「第一次収縮・安置外回収優先」と表示する。

## v1.13.47: 長い迂回ルートの抑制

- 候補比較で通常戦闘・通常イベントの累積個数を経路効率より先に比較していたため、同じ宝箱へ向かう場合でも遠方のイベントを拾う長い環状経路を選ぶことがあった。
- 宝箱、狂信者、邪教祖、確定高価値イベントなどの上位目標数は維持する。同じ上位目標を回収できる候補間では、通常活動数より先に「実移動＋退避＋瘴気ダメージ」の価値密度を比較する。
- `evacuationDistance`に含まれる帰路edgeへ`retrace * 2`を再加算していた重複を削除した。往路再利用は実距離ではなく、同程度の候補を選ぶ後段tie-breakとしてのみ残す。
- 橋edgeの袋小路は通常空白2個相当として扱い、短い継続経路より優先されないよう維持する。
- 邪教祖後に前方価値ノードへ続く既存規則は効率比較より優先し、特殊連続目標を途中で打ち切らない。

## v1.13.46: 収縮円倍率の実測取得と特殊イベント座標のMAP共通化

- `limitCircle.position`は拡縮後画像の左上であり、`center_position - position`を半径として扱わない。`page-hook.js`が`Game.view`以下のCreateJS表示オブジェクトを探索し、円画像の`scaleX/scaleY`、元画像寸法、bounds、transformed boundsを採取する。
- 当時は実表示の幅・高さを取得できた場合だけ座標シミュレーションを行う設計だった。この要件はv1.13.55で廃止し、登録済みOPFS実測patternモデルへ復元した。誤った670px固定円は引き続き使用しない。
- 特殊イベントの永続キーを`map|x|y|event`から`x|y`へ変更した。既存記録は読込時に座標単位へ統合する。
- map ID、base pattern、pattern、node IDはイベント識別条件ではなく`appearances`配列へ出現観測として保持する。出現時期は`initial-map`または`post-day1-boss`として記録する。
- 固定event 10の辞書判定もmap/node IDへ依存せず、XY `(813,332)`とevent IDで照合する。

## v1.13.45: 瘴気通行をパーティHP基準へ変更

- `/rest/arcarum3/dungeon/party_status`の`hp/max_hp`と、`action_scenario_list`の`after_party_status`を`routeRuntimeCache.partyMembers`へ保持する。
- 固定2回制限は廃止。推奨経路と退避経路の累積瘴気ダメージが、最もHP割合の低い生存メンバーを死亡させない場合だけ許可する。整数切上げ誤差用に計画範囲ぶんのHP余白を確保する。
- 保存ログで実測できた瘴気ダメージは`action_type:301`、`level:1/status:1`、最大HPの5%（端数切上げ）。第一次収縮完了後と1日目の第二収縮はユーザー実測で15%据え置き。
- 強度段階は、収縮中の`level/status/remainTurn`から完了済み収縮数を求める。第1収縮中だけ5%、第1収縮完了後は15%。到着前にcountdownが0になる経路では15%を使う。
- 第二収縮（`level >= 2`または`status >= 3`）では、予定瘴気ダメージ後も全生存メンバーが最大HPの85%以上かつ絶対HP60,000以上を保つことをhard constraintとする。ボス戦前のため、単なる生存可能判定より大きな余力を確保する。
- `action_type:301`の前後HP差から段階別率を自動学習し、`miasmaDamageRates`へ保持して既定値より優先する。

## v1.13.44: 現在イベントの取得と特殊イベント分類を修正

- `move_node` の到着先がnodeType 10なら、最新進入イベントとしてmap・XY・node・`special_incident_id`を即時保持する。後続の`action_scenario_list`受信時は同じ記録へsignalsを追記する。
- 最適ルート下部の特殊イベント欄は、履歴全体のコピー・JSON保存を廃止し、「今のイベントをコピー」で最新1件を報告形式の2行テキストとしてコピーする。
- 実ログの`node_list`には`special_node_type`がなく、`special_incident_id`が分類キーでもある。正規化時にこれを`specialType`へ反映し、1=邪教祖、2/3=狂信者、5/6/7=浮遊城への転移、8=浮遊城の研究者として専用表示と経路規則を適用する。

## v1.13.43: 予測比較実験を座標シミュレーションへ変更

この節の`center_position - limitCircle.position`による半径計算は、v1.13.46で実表示倍率・boundsによる計算へ置き換えました。

- UI名を「第一次収縮・座標シミュレーション」へ変更。実測瘴気、予測不一致、一致率、登録済みpatternモデルの表示を削除した。
- ランタイムの `FIRST_SHRINK_MODELS` を削除。`simulateFirstShrinkArea()` はOPFS/画面ログや `node.isShrinking` を参照しない。
- 開始中心は全ノードXYのmin/max中央。開始半径は開始中心から最遠ノードまでの距離+1。
- 最終中心は取得した `center_position`。最終半径はX/Yの `center_position - limitCircle.position` が2px以内で一致する場合の平均、または取得済み `circleRadius`。
- 現在中心・半径はstep/100で開始値から最終値へ線形補間する。現在円外を濃紫で塗り、円外ノードを `simulated-miasma` として表示する。
- 必須座標が不足する場合はnullを返し、過去のログモデルにはフォールバックしない。ルート計画の返却名は `firstShrinkSimulation`。

## v1.13.42: limitCircleからの第一次収縮半径取得

以下は当時の観測記録です。`center_position - limitCircle.position = 670`を半径とする解釈は、画像倍率が変わるパターンへ対応できないためv1.13.46で廃止しました。

- map 2 / base pattern 1 / pattern 1の `miasma_visual` で、中心 `(893,729)`、`limitCircle.position (223,59)`、画像 `miasma_circle_1.png` を取得した。X/Yとも差が670なので、positionは円画像の左上、第一次収縮の最終半径は670と直接判定できる。
- 新しいmap 3ログは `base_pattern_id: 2 / pattern_id: 1`、中心 `(1912,673)` の別パターン。step 30～100の収縮ノード差分を独立適合すると最終半径約668.1となり、limitCircle由来の670と約2px差で整合した。
- `captureRoutePlanningState()` は `circlePositionX/Y`、`circleRadius`、`circleImage` を返す。page側の円位置とAjax runtime側の中心が別々に得られた場合も `mergeRouteStates()` が半径を導出する。
- `predictFirstShrinkArea()` は `circleRadius` を較正済み固定値より優先する。未登録patternでも中心、半径、stepがあれば `modelKey: limitCircle` として予測円を生成する。
- 既存ログのmap 3 / base 2 / pattern 1では、`miasma_visual`が収縮前の2件しか保存されなかった。原因は古いpage route stateだけで重複判定していたこと。v1.13.42では現在Ajaxのmiasma情報を識別子へ使い、署名へ`effectCjsOption`と`limitCircle`も含めた。
- 4ファイル中、`17-51-05`はmap 2 / 1:1でvisual 3件、`17-57-28`はmap 3 / 2:1でvisual 2件、残る短い2本はAjaxのみ。次の採取ではmap 3 / 2:1の収縮中`miasma_visual`に `limitCircle.position`が保存されることを確認する。

## v1.13.41: 中心ノード仮説の撤回

- 収縮中心が特定ノードに設定されるという推測は、追加目視により偶然の一致と判明したため撤回する。中心はノード以外にも置かれる独立座標として扱う。
- Ajaxのmiasmaオブジェクトにあるキーは `is_miasmic / step / miasma_stop_countdown / level / status / cjs_name / bgm / base_pattern_id / pattern_id / center_node_id / center_position_x / center_position_y`。`center_node_id` は全記録でnullであり、中心とノードを結びつける情報は確認できない。
- Ajaxログ内には `circle / radius / limitCircle / effectCjsOption` は存在しない。白い半透明円の半径・描画設定は `window.Game.view.data.miasmaEffectInfo` 側にある可能性が高い。
- `page-hook.js` は各Ajax後に `miasmaEffectInfo` の全own propertyとサーバー中心を採取する。ノード最近傍候補やアンカー補正による中心ノード推定は保存しない。
- `background.js` はこれを `kind: 'miasma_visual'` としてOPFS JSONLへ保存する。同一step・status・中心の重複は除外し、画面用60件履歴には混ぜない。次の収縮ログでは、このレコード中の `effectCjsOption`、半径・scale・position系フィールドを確認する。

## v1.13.39: base pattern 1 / pattern 1 のMAP共通第一次収縮モデル

- 新規OPFSログ2本は、map 3とmap 1で異なるが、どちらも `base_pattern_id: 1 / pattern_id: 1`。異なる2パターンの資料ではない。
- 両ログともサーバー中心は `(893, 729)`。ノード座標系では最終中心を `(843, 629)`（補正 `-50, -100`）、完了半径を650とすると、各MAPの最終 `is_shrinking` 153件を100%分類する。
- 途中は `radius = 1600 - 9.25 * step`、step 100またはlevel 1/status 2では半径650を使用する。map 3は2,448判定中2,437一致（99.551%）、map 1は2,295判定中2,283一致（99.477%）、合計4,720/4,743（99.515%）。
- `FIRST_SHRINK_MODELS['*:1:1']` として登録し、完全キーのモデルがない場合に `basePatternId + patternId` の共有モデルへフォールバックする。既存 `4:3:1` は完全キーが優先される。
- 解析再実行用に `analyze-shrink-logs.cjs` を追加。JSONL永続ログと画面用JSON exportの両方を読み、`--compact`、`--fit`、`--circles` を利用できる。
- 異なるパターン間の一般式を確定するには、`1:1` 以外の `base_pattern_id / pattern_id` を持つ追加ログが必要。

## Workspace

新しいセッションでは次をWorkspaceに指定してください。

`D:\Dropbox\scripts\#GBFtools\testExtension`

開発対象はWorkspace直下です。Tarou配下のコピーは参照・変更しません。

## 最初に行うこと

以下の順に読みます。

1. `HANDOFF.md`
2. `README.md`
3. `docs\ARCHITECTURE.md`
4. `docs\TESTING.md`
5. 必要になった場合のみ `docs\INTEGRATION.md`

次に、Workspaceルートで実行します。

```powershell
node unit-test-v2.cjs
```

## 現在の状態

- 拡張機能バージョン: `1.13.114`
- ManifestのService Worker: `service-worker-v2.js`
- 既存戦闘情報表示の実装・直近のUI整理: 完了
- 模擬scenario単体テスト: 成功
- JavaScript構文・Manifest参照検証: 成功
- Dropbox競合コピー: なし
- GBF実戦テスト: 未完了
- `moveTIPS` への統合: 未実施
- hit予測: 完全削除（旧タブは導本リストとして再利用）
- デバフカウント: 再設計までオミット

直近の `node unit-test-v2.cjs` は `passed: true`。同一戦闘保持、リロード復元、全体・キャラクター別の現在→前ターン遷移、行動ごとの加算、新規戦闘リセット、万・億短縮表示、導本候補の全件取得と導本UIを確認済みです。

2026-07-23の最終確認では、Codex bundled Nodeを使った `unit-test-v2.cjs` が `passed: true` でした。環境のPATHに `node` がない場合は `C:\Users\ADMIN\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe unit-test-v2.cjs` を使用できます。このWorkspace自体はgit repositoryではありません。

## 2026-07-23時点の要約

現在の主作業は「最適ルート」機能の実戦調整です。計算本体は `route-planner.js`、ライブ状態の統合は `background.js`、SVG表示は `sidepanel.js` / `sidepanel.css` にあります。詳細仕様と実測根拠は `docs\ROUTE_PLANNING.md`、手動確認項目は `docs\TESTING.md` に記録しています。

直近で完成している重要項目は次のとおりです。

- 通過した通常戦闘・通常イベント・宝箱・回復を、公式再取得前でも空白化する。通常戦闘は到着・戦闘開始時点で先行空白化し、戦闘中も次ルートを計算する。
- 強敵・君臨者・超強敵から撤退して通過した場合は種別表示を残しつつ、探索上は空白相当として扱う。
- 実座標半径200～1600で表示し、半径内だけで現在地から連結するノードに限定する。現在地は空白円＋最前面・完全不透明の赤い地図ピンで表示する。
- 表示範囲外へ通常edgeが続く場合、推奨・非推奨を問わず全ての境界ノードから最大60pxの短い線を表示する。推奨方向は金色を後描画する。
- 空白数、実移動の折り返し、橋edgeによる袋小路、退避経路の往路再利用を評価する。価値候補が存在する場合、空白だけで終わる短い探索prefixは最終案にしない。
- 瘴気侵入は回数ではなく、全生存メンバーの現在HPと段階別最大HP割合ダメージで判定する。累積ダメージ後に誰かが死亡する経路は除外し、生存できる範囲では価値回収と比較する。
- 第一次収縮の実験表示は登録済みOPFS実測patternモデルによる座標シミュレーション。全ノード、現在計算円、最終安置円、シミュレート瘴気を表示する。
- 第二収縮では即時に最短ボス経路へ固定せず、`remainTurn - 1` を到着予算として、ボス最短距離を確保した残りで最大8手の価値寄り道を探索する。同じ枝を往復して現在地点へ戻る動きを許可し、余裕消失時は最短ボス経路へ切り替える。
- 狂信者は通常戦闘より高優先。全狂信者の後だけ邪教祖を許可し、邪教祖後は空白2連続までの先に未訪問非空白が必要。3連続空白が必要なら除外する。
- 浮遊城の研究者（specialType 8）は経路グラフから完全除外。浮遊城への転移（5/6/7）は通常時のみ1/3の低い期待報酬を持つ。第一次収縮の残り10ターン以下と第二収縮中はランダム転送リスクのため経路グラフから除外する。
- 訪問済みの1日目ボスと、完了後に残留した `bossNodeId` はボス候補から除外する。`finish_node_event.is_visited_node: true` をリロード前に即時反映する。
- ショップはドル袋 `💰`、瘴気ノードは濃紫半透明overlay、推奨ノード・edgeは発光なしの明瞭な金色輪郭で表示する。

現時点で最重要なのは、上記の優先順位が実マップの分岐で意図どおりになるか継続観測することです。特に空白回避、袋小路回避、HP余力内の瘴気経由による価値回収は相互作用が強いため、誤案内が出た場合はスクリーンショットだけで決め打ちせず、その時点のルート状態JSONまたはOPFS/Ajaxログを保存して模擬fixtureへ落としてください。

## 現在の開発段階

戦闘情報表示の基本実装は維持しつつ、現在は最適ルートの実機確認と導本効果一覧の採取を進めています。次に確認すべきなのは、実マップで推奨経路を数手進めた後も空白・瘴気・袋小路・折り返しの評価が安定することと、導本選択肢を開くだけで未選択候補を含む全件が保存されることです。デバフカウント再設計は、ユーザーから再開指示があるまで保留します。

サイドパネルの「新機能テスト」タブでは、`window.Game.view.data` 以下の全own propertyを階層制限なしで取得できます。タブを開いた時に自動取得し、手動の再取得・コピー・JSON保存にも対応します。循環参照やDOM要素を安全なJSON表現へ変換し、結果は容量対策のため拡張ストレージへ保存しません。実機で保存したJSONをWorkspaceへ置けば、次セッションのCodexが実構造を直接確認できます。

同タブに「Ajax状態更新履歴」を追加しました。「記録開始」で以前の履歴を消去し、その後のAjax URL・要求・応答をタブ単位の `chrome.storage.session` へ最大60件・合計約4MB記録します。現在ノード、経過ターン、所持通貨、訪問済み、収縮・瘴気関連フィールドを候補として自動抽出し、履歴の展開、更新、停止、クリア、コピー、JSON保存ができます。追加の実機作業ではフェーズ遷移やショップ利用を個別に記録し、未確定フィールドを確認してください。

実履歴から、通常移動は `/rest/arcarum3/dungeon/move_node` の `before_current_node_id`、`after_current_node_id`、`total_turn`、`miasma_info.before/after` で追跡できると確認しました。バージョン1.11.2でこれらを移動元・現在地候補として抽出し、`node_list[].is_shrinking` はtrueだけを表示するよう改善しています。戦闘後は `/arcarum3/dungeon/content/index/0` の `option.dungeon` で現在地・ターン・通貨・全ノード状態を再同期する方針です。詳細は `docs\ROUTE_PLANNING.md` を参照してください。

バージョン1.12.0では長時間計測用のOPFS永続ログを追加しました。画面表示用キャッシュは直近60件のままですが、完全なAjaxは受信ごとにChromeプロファイルのAppData配下へ即時追記されます。アプリ側の件数・総容量上限はなく、約5MB単位のJSONLへ自動分割します。新機能テストタブで一覧更新、各ファイルの保存、確認付き全削除ができます。上限は端末の空き容量です。

バージョン1.13.0では「新機能テスト」の隣に「最適ルート」タブを追加しました。ページから軽量なルート状態を取得し、Ajaxで現在地・ターン・収縮・ボス配置をライブ補正します。現在地から4ホップをSVG表示し、第一収縮中は安全期限と退避可能性を考慮した外周価値回収、第二収縮中はボスへの安全な最短経路を表示します。計算本体は `route-planner.js` で、模擬マップ単体テストを追加済みです。

バージョン1.13.1では最適ルートのノード表示をIDから絵文字へ変更しました。通常マスはnodeType、特殊マスは実データで確認したspecialNodeTypeに基づいて固有の背景色とアイコンを割り当て、IDはツールチップ内だけに残しています。

バージョン1.13.2では4ホップ局所マップを大型化しました。狭いサイドパネルへ縮小せず横スクロール可能な760px以上のキャンバスとし、座標間隔、ノード径、絵文字、通常接続線、推奨線を拡大しています。

バージョン1.13.3では大型キャンバスを取りやめ、1.13.1の表示寸法へ戻しました。接続線の見える面積を増やすため、ノード円を通常16・現在地20、絵文字を14pxへ縮小しています。

バージョン1.13.4では回復マスの絵文字を十字から薬（💊）へ変更しました。背景色はビリジアングリーンのままです。

バージョン1.13.5では、収縮前から退避リスクを経路評価へ含めました。収縮先が未確定の間はマップ中央寄りの約35%を仮の安全コアとし、各候補からコアまでの距離、往路edgeの再利用数、来た方向以外の安全な出口数を算出します。3edge以上を完全に引き返す袋小路は候補から除外し、同じ回収価値なら回収経路と退避経路の合計効率が高く、別方向へ抜けられるルートを優先します。収縮開始後は実際の恒久安全ノードをコアとして同じ評価を再計算します。

バージョン1.13.6では転移edgeを計算用グラフと表示用グラフに分離しました。経路計算は転移先を評価しますが、転移前の4ホップ描画と手順は転移元で打ち切り、「転送する」または「転送しない」だけを表示します。転移先のノードと周辺エリアは、Ajaxで現在地が転移先へ更新された後に初めて表示します。

バージョン1.13.7では `/proceed_node_event_select_warp` の実ログを反映しました。要求 `select: 0` は転送拒否、`select: 1` は転送実行で、応答自体には現在地IDがありません。拒否時は現在の転移元を保持してその転移edgeを離脱まで無効化し、実行時は2つの転移マスから相手側を現在地として即時更新します。nullを数値0へ変換してしまうルート用数値正規化も修正しました。

バージョン1.13.8では4ホップマップの座標基準を現在地へ変更しました。実座標がある場合も代替配置の場合も現在地をSVG viewBoxの中央 `(300, 200)` に固定し、周辺ノードを現在地からの相対座標で配置します。

バージョン1.13.9では、現在地中央表示で描画倍率が重心中央の75%未満になるマップ端だけ例外とし、表示ノードの重心を中央に置いてキャンバスを有効利用します。推奨手順を読みやすく詰め、各手にXY座標・node ID・特殊event IDを表示します。特殊イベントはmap/XY/eventをキーに `chrome.storage.local` へ継続記録し、実際に訪問したAjax応答の識別候補も統合してコピー・JSON保存できます。未分類のイベント内容は推測せず、この採取データから無ターン複数戦闘イベントを確定する方針です。1日目は特殊イベントを避け、ターン50以降を2日目として候補に含めます。

バージョン1.13.10では、`move_node` の移動元を離脱済みとしてタブ内に保持し、戦闘・通常イベント・宝箱・回復マスを推定空白化します。古い `node_list` に元の種別が残っていても再適用します。特殊イベント、強敵系、ボス、ショップ、転移は通過だけでは消化済みにせず、公式の `is_visited` を待ちます。マップIDが変わった場合は推定状態を破棄します。

バージョン1.13.11では、推奨経路へ折り返しペナルティを追加しました。直前の辺を戻る動きと、別の安全な分岐がある状態で戻るしかない行き止まりへ入る動きを強く避けます。唯一の安全経路や転移経路では必要な折り返しを許可します。表示範囲は4ホップ固定ではなく、現在地からの実座標距離による円形範囲です。サイドパネルで半径200～1600（初期値600）を設定でき、`chrome.storage.local` に保存します。座標欠損時のみ4ホップへフォールバックします。

バージョン1.13.12では、ショップアイコンをCSSで重ねた2枚のコイン `🪙🪙` に変更しました。推奨手順の各行へ `route-kind-*` クラスを付け、順番数字の背景色をマップ上のノード種別色と共通化しています。

バージョン1.13.13では空白回避をルート比較の最上位へ移動しました。訪問済み空白を再訪した場合も `empty` と `revisit` の両方へ加算し、空白数、折り返し、回収価値の順に比較します。回復マス（nodeType 7）は空白ではないため `empty` へ加算しません。空白を避けられる候補があれば、空白が続く滑らかな経路より優先します。

バージョン1.13.14では、推奨経路の最初の「半径内ノード→半径外ノード」遷移を検出し、半径内の終端ノードから実座標方向へSVG外周まで継続線を描きます。座標欠損時は直前の推奨線方向へフォールバックします。範囲外ノードのアイコンは描画せず、転移前は `displayPath` が転移元で終わるため別エリアへの誤った継続線も出ません。

バージョン1.13.15では、半径条件を満たすノードだけの部分グラフを作り、その中で現在地から到達可能な連結成分だけを表示します。全体グラフではつながっていても、接続経路が一度半径外へ出る近接ノード群は遠隔クラスタとして除外します。

バージョン1.13.16では、現在地だけをSVGの地図ピン型 `path` で描画し、その他のノードは円形を維持します。種別ごとのfill/stroke、推奨発光、瘴気、ボス、現在地の緑枠を `.route-node-shape` 共通クラスへ適用しています。現在地の絵文字はピン上部へ7px移動しています。

バージョン1.13.17では継続線の条件漏れを修正しました。明示的な「半径内→半径外」の推奨ステップに加え、推奨手順の終端が半径内にあり、その終端ノードから表示用グラフ上で半径外へ接続している場合も接続先を選んで継続線を描きます。複数接続がある場合は直前の進行方向に最も近い接続を選びます。転移ノード間の仮想edgeは対象外です。

バージョン1.13.18では継続線の長さを、ノード中心から最大60 SVG単位へ制限しました。表示端までの距離が60未満の場合のみ外周で切り、それ以外は外方向が判別できる短い線として描画します。

バージョン1.13.19では、推奨ノードと推奨edgeの `drop-shadow` を削除しました。推奨ノードは種別色のfillを保ちながら金色 `#fbbf24`・太さ5のstrokeで囲みます。現在地は緑色 `#4ade80`・太さ5のstrokeを優先し、影を付けません。

バージョン1.13.20では、現在地ノード自体をピンへ変形する方式を廃止しました。現在地の土台を常に空白マスの円・点表示にし、その上方へ赤色 `#e53935`、白い中心穴を持つ独立したSVGピンを配置します。ピン先端は空白マス円の上端へ接触します。現在地は推奨ノードの金枠から除外し、元のノード種別はtitleへ保持します。

バージョン1.13.21では、現在地ピンを現在地ノードの `g` から分離し、全ノードの描画後に `route-current-pin-overlay` としてSVG末尾へ追加します。これによりDOM描画順で常に最前面になります。オーバーレイは `pointer-events: none` です。

バージョン1.13.22では、ピン本体・中心穴へ `opacity: 1`、`fill-opacity: 1`、`stroke-opacity: 1`、オーバーレイへ `opacity: 1` を明示し、訪問済みノードの半透明表示に影響されないよう固定しました。

バージョン1.13.23では、通常戦闘（nodeType 2）だけを移動到着時に即座に推定空白化し、`/rest/(raid|multiraid)/start.json` 検知でも同じ処理を補完します。戦闘画面でページ状態取得が失敗した場合は `routeRuntimeCache` の地図を返すため、戦闘中も次ルートを表示できます。強敵・君臨者・超強敵（nodeType 3/4/11）は到着では処理せず、そこから移動した時点で `passedDangerNodeIds` へ記録します。種別表示は残し、`isPassedDanger`・`isVisited` を付けて探索上の `empty` を1加算します。

バージョン1.13.24では現在地ピンの形状を18 SVG単位下へ移し、尖った先端を現在地の空白マス中心 `(point.x, point.y)` に一致させました。ピン本体の縦寸法、完全不透明、最前面オーバーレイは維持しています。

バージョン1.13.25では「新機能テスト」に第一次収縮の最終半径解析用取得UIを追加しました。`mapId`、`basePatternId`、`patternId`、level/status/step、円中心、全ノード座標・`isShrinking`・中心距離、`miasmaEffectInfo` と `miasma_info` の全own property、Ajax由来の最新ルート状態を同一JSONへ保存します。level 1かつstatus 2またはstep 100では、安全ノード最大距離を下限、収縮ノード最小距離を上限として半径範囲を自動算出します。

バージョン1.13.26では瘴気内への侵入を最大2回まで許容します。各到着ターンがノードの安全期限以上ならダメージ1回として数え、推奨経路で使用済みの回数と、その地点から安置へ退避するために必要な回数の合計が2以下の候補だけを残します。同価値なら侵入0回を優先し、ボス到達も0回、1回、2回の順で探索します。3回以上必要な経路は除外します。

バージョン1.13.27では瘴気内ノードの赤い点線strokeを削除し、半径15.5の `route-node-miasma-overlay` をノード形状と絵文字の後に追加します。濃紫 `#3b0764`・不透明度0.72で内容を覆い、推奨外周線が見える大きさに留めています。現在地ピンは全ノード後の専用オーバーレイなので引き続き最前面です。

バージョン1.13.28ではmap 4・node 9・XY `(813, 332)`・event 10を `CONFIRMED_FIXED_SPECIAL_EVENTS` に登録しました。選択肢は1つで対価なし、HP全回復、全アビリティ・召喚石即時使用可能化、固定導本効果「味方全体のアビリティ使用間隔を1ターン短縮 / バトル開始時に味方のHPを10%回復する」を獲得します。一般特殊イベントの1日目除外を適用せず、`special: 1` の高価値候補として扱います。

バージョン1.13.29では最適ルート下部に全153ノードを描く第一次収縮比較MAPを追加しました。OPFSログ `2026-07-22T14-38-44-345Z` のmap 4・base pattern 3・pattern 1をモデル化し、開始中心をノードXY範囲の中央、最終中心をサーバー中心 `(904, 1082)` から補正した `(867.9, 1000.5)`、半径を `1655.9043615 - 9.89789339 × step` とします。step 45/50/75/90/95/100の実測一致率は99.3/100/99.3/99.3/98.7/100%です。MAPには現在予測境界、最終安置、実測瘴気、不一致ノードと一致率を表示します。未登録patternでは全ノードだけ表示して警告します。

バージョン1.13.30では狂信者（nodeType 10 / specialType 2, 3）へ通常戦闘より高い `fanatic` 優先度を付けました。邪教祖（nodeType 10 / specialType 1）は、全狂信者IDが候補の訪問済み集合へ入った後だけ `cultLeader` 候補になります。さらに邪教祖の進入元edgeを除外した探索で、未訪問かつnodeType 0以外のノードへ到達できなければ邪教祖候補自体を除外します。狂信者・邪教祖は戦闘系として1日目から経路へ含めます。

バージョン1.13.31では邪教祖後の価値経路探索に連続空白数を持たせ、空白2マスまでは通過可能、3マス連続した時点でその探索枝を打ち切るようにしました。したがって2空白の先に未訪問の非空白ノードがあれば邪教祖を許可し、到達に3空白以上が必要なら除外します。

バージョン1.13.32ではボス候補を未訪問ノードだけに限定しました。第一次ボス撃破後も `miasma.bossNodeId` が残留する場合があるため、対象ノードが訪問済みならボスモードへ入らず、価値探索へ切り替えます。これにより撃破済みボスや空白化した進入路へ戻る経路を再採択しません。

バージョン1.13.33ではショップのノードアイコンを `🪙🪙` からドル袋の `💰` へ変更し、複数コイン表示用だった負の文字間隔を削除しました。

バージョン1.13.34では現在地から出る各edgeが橋かを判定し、複数の進路があるのに、そのedgeを通らなければ外へ戻れない角・袋小路の枝へ入る候補へ `trappedBranch` を付けます。これは将来空白化する往路1マス相当として実空白数へ加え、同数なら非袋小路を優先します。遠隔地へ接続する転移マスは袋小路判定から除外します。

バージョン1.13.35では浮遊城の研究者（nodeType 10 / specialType 8）を経路グラフから除外しました。浮遊城への転移（specialType 5/6/7）は一般特殊イベントの1日目除外を適用せず通過可能にし、研究者を使わない場合は3箇所中1箇所が正解で最高レアリティ導本効果を得るため、`1/3` の低い期待報酬だけを効率値へ加えます。通常戦闘・通常イベントの `activity` 優先度は付けません。

バージョン1.13.36では実効空白数が同数の場合の比較を、折り返しpenalty、回収価値、袋小路、瘴気侵入回数の順へ変更しました。また3edge以上を全て戻る候補のhard exclusionを廃止し、`trappedBranch`、`retrace`、`evacuationDistance` によるsoft penaltyへ統一しました。探索中は価値を1つ以上回収する `best` と空白のみでも許す `bestFallback` を分離し、価値候補が存在するのに短い空白prefixだけを最終案にする挙動を防ぎます。侵入・退避合計2回の上限は維持し、瘴気1回＋空白1マスで複数イベントを回収できる経路が空白のみの退避ルートに勝つテストを追加しています。

バージョン1.13.37では `routeOffRadiusContinuationSegments` を追加し、表示範囲内の全ノードから範囲外へ出る通常edgeを最大60pxの通常色stubとして描画します。推奨経路の範囲外継続線は後から推奨色で重ねます。描画方向には範囲外ノードのXYを使用し、範囲外ノード本体は表示しません。

バージョン1.13.38では第二収縮専用の `searchBossRouteWithDetours` を追加しました。`remainTurn - 1` までにボスへ着くこと、瘴気侵入合計2回以内をhard constraintとし、最大8手の寄り道候補から宝箱、狂信者、確定特殊、通常イベント・戦闘などを比較します。この探索では通常の折り返しpenaltyを適用せず、同じnodeを2回まで通る往復を許可します。表示modeは寄り道中が「ボス到達前の安全な寄り道」、余裕がない場合は「ボス到達優先」です。

## 実装済み要件

### リロード耐性と表示調整

- 同一 `raid_id` の状態を永続ストレージから復元する。
- キャラクター別表示を名前・見出し・数値の3行にする。
- キャラクター別ダメージも万・億単位で短縮する。
- 全体の縦間隔を詰める。
- ターン統計を見出し・数値の横一列表示にして縦幅を圧縮する。
- 貢献度順位を色分け順位バッジ、名前、貢献度に分離する。

### デバフカウント

- 現行方式は実機で正しく機能しないため、バージョン1.6.1ではオミットした。
- AJAX結果とWebSocketから旧デバフ解析処理を呼び出さない。
- デバフ検証タブとキャラクター別デバフ表示を削除した。
- 保存済み状態との互換性のため旧フィールド・解析ヘルパーは残すが、集計には使わない。

### 導本効果リスト

- 導本選択肢の`action_type: 401`にある`status_list`を、獲得操作の有無にかかわらず全件収集する。
- 戦闘リザルトの`arcarum3RewardList`にある`rewardType: 4`も収集する。
- `guidebook-effects:v1`へID単位で保存し、`name`を効果本文として扱う。
- 「導本リスト」タブでID、名称、レアリティ、重複可否、観測時刻・回数を表示する。
- `@@`を改行して効果名を表示する。ID・効果名フィルターとJSONコピーを利用できる。
- Ajax受信中にタブを開いていれば`GBF_GUIDEBOOK_EFFECTS_UPDATED`で即時更新する。
- hit予測に使っていた奥義ゲージ・バフ取得、予測計算、履歴、UIは残していない。

### 与ダメージ合計

- 戦闘全体で累積する。
- ターン進行ではリセットしない。
- 同一 `raid_id` の `start.json` を再受信しても保持する。
- 別の `raid_id` が開始した場合だけ0へ戻す。

### 現在・前ターン

- アビリティなどの結果受信ごとに `currentTurn` へ加算する。
- 通常攻撃でターンが進む際、最終的な `currentTurn` を `previousTurn` へ移す。
- 移動後の `currentTurn` は0から開始する。
- `previousTurn` は次のターンが確定するまで保持する。

### キャラクター別

- 現在ターンと確定済み前ターンのhit数、アビリティダメージ、奥義ダメージを表示する。
- `frontFormation` に含まれる現在のフロントキャラクターだけを、formationの順序で表示する。
- キャラクター一覧全体を独立した「現在ターン」と「前ターン」の2テーブルに分けて横並びにし、同じフロント順でhit・アビリティ・奥義を配置する。
- キャラクター名は現在ターン側だけに表示し、前ターン側は同じ高さの空欄で左右の行を揃える。
- 復元状態を含むキャラクター配列のindexと数値を正規化し、キャラクター別前ターン値が0へ欠落しないようにする。
- 各テーブルのhit列を3桁幅に固定し、アビリティ・奥義へ広い幅を配分する。
- `action.num`、直前のability/specialコマンド、`windoweffect` のPIDを利用して所有者を判定する。
- 不明なダメージを先頭キャラクターへ誤帰属させない。

### 貢献度順位

- 上位6名を2列で表示する。
- 名前と数値を改行する。
- 1万以上は `n.n万`、1億以上は `n.n億` とする。

## 次セッションの優先作業

1. Chromeへ `D:\Dropbox\scripts\#GBFtools\testExtension` を読み込む。
2. `unit-test-v2.cjs` を実行し、`passed: true` を確認する。
3. GBF実戦で [docs/TESTING.md](docs/TESTING.md) の最適ルート項目を優先して確認する。
4. 推奨経路が不自然な場合は、その時点の画面、ルート状態JSON、Ajax履歴またはOPFSログを保存する。
5. 保存データから `currentNodeId`、全nodeのtype/specialType/visited/shrinking/adjacency/XY、miasma、推奨pathを再現し、`unit-test-v2.cjs` にfixtureを追加してから修正する。
6. 特に「空白だけの短いprefix」「価値の多い瘴気1回ルート」「橋edgeの価値ある枝」「ボス撃破直後」「範囲外接続線の全方向表示」を重点確認する。
7. キャラクター別2テーブルなど既存戦闘UIも余裕があれば [docs/TESTING.md](docs/TESTING.md) に沿って確認する。
8. 導本選択肢を表示し、未選択候補を含む全件保存・効果名表示・再読み込み後の復元を確認する。デバフカウントは再開指示まで拡張しない。
9. 実戦確認完了後のみ、[docs/INTEGRATION.md](docs/INTEGRATION.md) に従って `moveTIPS` との統合を検討する。

## 効率ルート算出の次期要件

取得したノード構成を使う効率ルート算出の仕様は `docs\ROUTE_PLANNING.md` に整理しています。ゲーム開始時に `isVisited: true` が1個だけならそのノードを開始地点とし、所持通貨は `window.Game.view.data.dungeonShopPoint` から取得します。

宝箱を高優先、狂信者を通常戦闘より高優先、通常戦闘・イベントを価値マスとして扱い、空白・再訪・通過済み強敵系を避けます。ショップは到達時期待通貨500/1000の条件があります。一般の未分類特殊イベントは原則として1日目に避けますが、確定event 10、狂信者・邪教祖、浮遊城への転移には個別規則があります。浮遊城の研究者は完全除外です。

特殊イベント `event 15` は、map 4の `node 14`（XY `1090, 994`）で内容を実測確認済みです。選択肢は、通貨150で導本効果を2つ失い2つ得る、通貨300でレアリティの最も高い導本効果を1つ得る、または退出する、の3つで、退出後も再入場できます。最適ルート提案の完成後に導本効果の評価機能を追加し、デメリット効果の個数と重みを計上してください。ルート評価では所持通貨、寄り道コスト、デメリット改善期待値を比較します。導本情報を取得できるまでは推測値で高優先化しません。詳細は `docs\ROUTE_PLANNING.md` の「確定イベント辞書: map 4 / event 15」を参照してください。

OPFS永続ログで、1日目第一収縮はターン9～29、第二収縮・ボス配置はターン38、1日目ボス討伐による収縮解除はターン50、2日目第一収縮はターン59～79、第二収縮・ボス配置はターン88と確認しました。第一収縮中は各ノードの瘴気到達ターンを安全期限として、外周の期限が早い価値ノードから回収し、処理後も安全圏へ退避できる時間窓付きbeam searchを使用する設計です。詳細とフォールバック条件は `docs\ROUTE_PLANNING.md` を参照してください。

## 作業範囲

- 変更可能: `D:\Dropbox\scripts\#GBFtools\testExtension`
- 変更禁止: `D:\Dropbox\scripts\#GBFtools\Chrome-Extension-Tarou`
- 実戦テスト完了まで変更禁止: `D:\Dropbox\scripts\#GBFtools\moveTIPS`

## Dropbox作業上の注意

- 同期による一時的なファイルロックが発生する。
- ディレクトリ全体の移動を避け、ファイル単位で編集・反映する。
- ロック時は強制上書きせず再試行する。
- 反映後はハッシュ、構文、競合コピーを確認する。

## レガシーファイル

- `unit-test.cjs`: v1.1向け。現在の正式テストは `unit-test-v2.cjs`。
- `service-worker.js`: キャラクター帰属の安全化層としてV2から読み込まれるため、削除しない。
- `background.js`: 基本集計実装としてV2から読み込まれるため、削除しない。
