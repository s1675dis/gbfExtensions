# 新セッション用Prompt

以下を新しいセッションの最初のPromptとして使用してください。

補足: 現在の最新バージョンはv1.13.126。GitHubの`update/channel.json`を6時間ごとに確認し、導本辞書・第一次収縮共有モデルはJSONで自動統合する。コード版更新はGitHub Releasesへの導線を表示する。詳細は`docs/ONLINE_UPDATE.md`を参照。撤退→再出撃はタブ別`guidebook-sortie-state:v1:<tabId>`のターン・訪問済みnode巻き戻りで検出し、導本辞書の現在所持数`count`だけを0へ戻す。派生`guidebook-effect-values:v1`は即時再生成する。名称・ID・レアリティ・解析・累積観測履歴は保持し、同じ探索のリロードではリセットしない。

補足: 現在の最新バージョンはv1.13.124。第一次収縮は`route-first-shrink-models:v1`の永続学習モデルを最優先し、未観測patternでは`limitCircle.position`と半径670から初期推定する。収縮完了時に安全／瘴気node境界を自動フィットし、map/base pattern/pattern/day単位で最大20観測の中央値へ更新する。フィールド250ms再同期とAjaxから自動実行し、補正済み円を第2収縮開始円へ渡す。手動採取は不要。

補足: 現在の最新バージョンはv1.13.123。「獲得導本効果」は効果確認ページの`count`で現在所持が確認でき、集計値が0ではない効果だけを表示する。`count: 0`と所持数未確認は表示・加算せず、未確認件数だけステータスへ残す。割合、追撃、固定値、個数、回数、特殊効果の空区分は「有効な効果なし」と表示する。現段階では戦闘条件の発動可否ではなく、現在所持中かどうかを「有効」としている。

補足: 現在の最新バージョンはv1.13.122。第一次収縮完了時、同じ安全性・価値・空白・折り返し条件の継続経路では、最終安置中心から遠い価値nodeを先に選び外周から内周へ回収する。bridgeや真の袋小路には適用しない。提供map 2 / T32 / node 68では旧`68→69`を`68→58`へ修正し、完全報告データの再投入でも確認済み。

補足: 現在の最新バージョンはv1.13.121。提供された99件の導本辞書を基準に、百分率、固定値、渾身・背水個数、召喚・通常hit回数、4種の読み替え、ID 62の複合効果を`guidebook-effect-values:v1`へ分解する。追撃は同一IDのみ加算し、異なるIDは`slotKey`で別枠表示する。現在所持数には効果確認ページの`count`だけを使い、全期間累積`acquisitionCount`は使わない。UIは割合、固定値等、特殊効果の3区分。

補足: 現在の最新バージョンはv1.13.120。R1導本本文から攻撃力UP・連撃率・アビ与ダメ・アビダメ・アビ上限の百分率を抽出し、`guidebook-effect-values:v1`へ別保存する。導本リスト上部で所持数反映後の合計を表示し、重複可は個数分、重複不可は1個分とする。効果確認ページの`count`を優先し、ない場合は実獲得回数を暫定利用する。R2/R3、渾身・背水等の条件式は未実装。

補足: 現在の最新バージョンはv1.13.119。浮遊城本体は既知の`XY 346,292 / specialType 4`だけでなく、`nodeType 10`で送出・流入edgeが一切ない完全孤立nodeとして判定し、未知の2箇所目も経路と特殊イベント座標記録から自動除外する。ランダム出現する狂信者（specialType 2/3）・邪教祖（specialType 1）も固定座標記録から除外し、通常戦闘＋小報酬として、空白を挟まず次の価値nodeへ連続できる場合だけ経路候補にする。

補足: 現在の最新バージョンはv1.13.118。転移しない場合は独立した判断行や見出し補足を出さず、現在地の転移nodeを推奨手順へ含めて`🚫転移（転移しない）`と表示する。詳細は`現在地・転移を拒否`とし、後続手順を続ける。

補足: 現在の最新バージョンはv1.13.117。第一次収縮中、最初の価値マスの処理完了まで4ターンを超える空白先行を`leadingValueDelay`として、期限付き報酬合計より先に抑制する。提供報告のmap 3 / T10 / node 57では、空白62→1→84→85を先行せず、隣接イベント51を先に回収してから外周回収へ戻る。

補足: 現在の最新バージョンはv1.13.116。固定特殊イベント`event 14 / XY 910,1419`を登録した。強敵1戦後に仲間（通貨300相当）または最高レアリティ導本を得る。1日目は強敵相当で積極攻略せず、1日目ボス撃破後は両報酬を高価値として通常戦闘より優先する。最低1処理ターンを加算する。

補足: v1.13.115では収縮シミュレーションMAPのボスnodeをマゼンタのhaloとドラゴン絵文字で強調する。ボスIDまたはnode type 1と座標を取得した時点で第2収縮シミュレーションを開始する。第一次最終円が保存されていない場合は、`isShrinking`が示す安全／瘴気node境界から現在円を復元し、その時点から残りターン分のボスアンカー相似縮小を再開する。両分類が揃わない場合だけ接続距離へフォールバックする。

補足: v1.13.114ではbase pattern 1 / pattern 1の実測境界（node 37が内、70・119・120が外）に基づき、登録済み座標モデルの中心(843,629)・半径650を画像外形由来の中心(893,729)・半径670より優先する。登録モデルがあるpatternではnative画像情報で最終安置円を上書きせず、既存のsaved-native-imageスナップショットも次回表示時にモデル円へ置換する。

補足: v1.13.113では固定特殊イベント`event 16 / XY 1597,1062`を、強敵連戦の可能性から1日目だけ`firstDayValue: 0 / treatAsDanger: true`とした。1日目は通常戦闘・通常イベントより優先せず、1日目ボス撃破後は従来の高価値へ戻す。最低2処理ターンの見積りは維持する。

補足: v1.13.112ではフィールド復帰再同期の署名へ全nodeのid/type/specialType/isVisited/isShrinkingを追加し、復帰直後に空白だったnode種別が後から確定した場合も再送する。通過による空白化は実際の`move_node`応答だけに限定し、非移動通信の一時的なcurrent_node_id変化ではイベントを消化済みにしない。

補足: v1.13.111では戦闘中に第1収縮が始まった場合、`#arcarum3/dungeon/`へ戻った後にフィールド状態を250ms間隔で再取得し、現在ターン、全node座標、収縮level/status/step/remainTurn、pattern、中心、limitCircleを戦闘前runtimeより優先して同期する。最適ルート表示中は即時再計画し、第1収縮円を生成・保存する。

補足: v1.13.110では`miasma_circle_1.png`を1340×1340・中心(670,670)として登録し、`limitCircle.position + (670,670)`がサーバー中心と3px以内で一致した場合だけ、半径670の確認済み画像円を第1収縮スナップショットへ優先保存する。

```text
このプロジェクトの継続作業を行ってください。

現在のバージョンは1.13.115です。第1収縮シミュレーションの最終安置円は、中心・半径と日・MAP・patternをスナップショット保存し、第2収縮で最優先の開始形状として流用します。保存値がない場合は、安全／瘴気nodeの観測境界から現在円を復元し、その時点から残りターン分の収縮を再開します。復元できない場合だけ接続距離へフォールバックします。ボスnodeのIDと座標を取得した時点でlevel/status更新前でもシミュレーションを開始し、MAP上ではボスをマゼンタで強調します。第2収縮は第一次最終安置円を開始形状とし、ボスnodeを円内の固定アンカーとして相似縮小します。ボスは中心ではなく、外周寄りに出た場合はその相対位置を保ったまま円中心がボス方向へ移動します。node期限は移動円との交差時点から算出します。固定event 18 / XY 2396,1466は強敵勝利後に20%割引ショップへ遷移し、1日目は強敵同様に回避、2日目は強敵価値へ昇格します。通貨500以上では`2^((currency - 500) / 500)`を連続加算します。固定event 9 / XY 989,648は強敵4連戦なので1日目の固定価値を0とし、1日目ボス撃破後だけ通常戦闘4回相当の高価値へ昇格します。候補・戦闘報酬・イベント・ショップ・効果確認から導本効果を辞書へ取得すると、表示中のタブに関係なくサイドパネル上部へ1効果につき1つの`ID + 効果名`トーストを、取得順に上から縦並びで表示します。これは実所持ではなく辞書回収成功の通知です。画面に効果が表示されたのにトーストが出ない場合は、表示を残したまま全タブ共通の「導本未取得をコピー」を押すと、DOM・Game.view・導本候補パス・現在辞書・記録済みAjax履歴を含む`gbf-guidebook-capture-failure-v1` JSONをCodexへ渡せます。導本タブのJSON保存／JSON読込で別PCへ台帳を移行できます。importは既存台帳を消さず、IDと固定化効果名で冪等統合します。リザルトの「探索画面に戻ることで～導本効果を獲得できます」は非効果案内として除外し、既存保存分も削除します。`(残りn/m回)`は初期値mの`remaining-count`変数として統合し、ID 57は常に`残り2/2回`の1枠です。第2収縮はボス到着5ターン余裕と瘴気侵入0を併用します。最適ルートSVGでは固定辞書へ登録済みの特殊イベントノード右上に緑色の「済」バッジを表示します。呪われた導本はisCursed/bookCategory/rarity 99/iconCategory 4から判定して赤紫で表示します。状況カウント系導本は辞書名を初期値0へ固定し、effectTemplate/effectVariables/lastObservedVariableValuesへ可変部分を分離しています。

Workspaceおよび変更対象:
D:\Dropbox\scripts\#GBFtools\testExtension

Chrome-Extension-Tarou配下の旧コピーは参照・変更しないでください。moveTIPSは実戦検証が完了し、ユーザーが統合を指示するまで変更しないでください。

最初に以下を順番に読んでください。

1. HANDOFF.md
2. README.md
3. docs\ROUTE_PLANNING.md
4. docs\TESTING.md
5. docs\ARCHITECTURE.md
6. 統合が必要になった場合のみ docs\INTEGRATION.md

次にWorkspaceルートで単体テストを実行し、結果を報告してください。

node unit-test-v2.cjs

`node` がPATHにない場合は、現在のCodex bundled runtimeで使われているNode実行ファイルを確認して同じテストを実行してください。直近の結果は `passed: true` です。このWorkspaceはgit repositoryではないため、`git status` を前提にしないでください。

現在の拡張機能バージョンは1.13.114です。主作業は「最適ルート」機能の実戦調整と導本情報収集の実機確認です。

主要ファイル:

- route-planner.js: ノード正規化、グラフ、収縮予測、beam search、優先順位
- background.js: Ajaxからのライブ状態更新、通過・消化済み推定、ボス完了反映
- sidepanel.js / sidepanel.css: 局所SVGマップ、推奨手順、現在地ピン、範囲外接続線、実験MAP
- unit-test-v2.cjs: 戦闘情報とルート機能の回帰テスト
- docs\ROUTE_PLANNING.md: 実測済み仕様と経路設計

現在実装済みの重要なルート仕様:

- 通常戦闘・通常イベント・宝箱・回復は通過後、公式更新前でも空白化する。通常戦闘は到着・戦闘開始時点で次ルートを先行計算する。
- `move_node`応答に現在地がない場合はrequestの`node_id`で補完する。宝箱が応答側ですでに空白へ変化していても、離脱前の種別から消化済みを維持して元の宝箱へ戻らない。
- 浮遊城本体の孤立ノード（type 10 / specialType 4 / XY 346,292）は演出上の一時座標。実座標は別途保持し、ルート計算では移動前の浮遊城転移マスを現在地として全MAP表示を維持する。
- 通過済み強敵・君臨者・超強敵は表示種別を残し、探索上は空白相当とする。
- 表示は現在地中心の座標半径200～1600。半径内ノードだけのグラフで現在地と連結するノードだけ表示する。
- 全ての表示ノードについて、通常edgeが範囲外へ続く場合はその全方向へ最大60pxの短い通常線を表示する。推奨方向は金色で上書きする。
- 現在地は空白円と、先端が円中心を指す最前面・完全不透明の赤いGoogle Maps風ピンで表示する。
- 空白数、折り返し、橋edgeによる袋小路、回収価値、退避経路を比較する。価値候補があるときは空白だけで終わる短い探索prefixを採用しない。
- 瘴気侵入は固定回数ではなくパーティHP基準。第1収縮中5%、第1収縮完了後と1日目第二収縮は15%として累積する。第二収縮では予定ダメージ後も全生存メンバー70%以上を必須とする。`action_type:301`の実測率があれば既定値より優先する。
- 1日目ボス完了はfinish_node_event.is_visited_nodeで即時反映し、訪問済みボスや残留bossNodeIdへ戻らない。
- 狂信者は通常戦闘より高優先。全狂信者の後だけ邪教祖を許可する。邪教祖後は空白2連続までの先に未訪問非空白が必要で、3連続空白が必要なら除外する。
- 実レスポンスの`special_incident_id`を特殊イベントの分類キーとして扱う。浮遊城の研究者（ID 8）は経路から完全除外。浮遊城への転移（ID 5/6/7）は1日目から通行可能で、1/3の低い期待報酬のみ付与する。
- 特殊イベントへ進入した瞬間のmap・XY・node・event IDは`specialEventObservation`へ保持され、サイドパネルの「今のイベントをコピー」で報告形式のテキストを取得できる。
- map 4 / node 9 / event 10は無償全回復などを得る確定高価値イベント。map 4 / node 14 / event 15は通貨と導本デメリット評価が必要。導本効果一覧の収集機能は実装済みだが、選択肢ごとのデメリット評価は未実装なので現在は推測で高優先化しない。
- 第一次収縮実験MAPは学習済みの完全キーを優先し、未観測patternには他patternを流用せず、その場の`limitCircle`座標から初期推定する。
- 第二収縮はremainTurnからボス最短距離と安全余裕1ターンを確保し、残り予算で最大8手の価値寄り道を行う。同じ枝を進んで戻る往復を許可し、余裕がなくなると最短ボス経路へ切り替える。

残りの主要作業・課題:

1. 実戦でルート優先順位を継続検証する。空白回避、袋小路回避、HP余力内の瘴気経由による価値回収は相互作用が強い。
2. 今後異なる日・難度で`action_type:301`を観測した場合、段階別自動学習値が既定の5%/15%と一致するか確認する。
3. 不自然な案内が出たらスクリーンショットだけで推測せず、同時点のルート状態JSONまたはAjax/OPFSログを取得し、unit-test-v2.cjsへ再現fixtureを追加する。
4. 第一次収縮モデルは自動学習する。新patternでは初期推定のずれを許容し、収縮完了時に`isShrinking`境界から学習モデルが追加・中央値更新されることを実機確認する。
5. event 15の本評価に必要な導本効果一覧の収集は実装済み。デメリット個数、レアリティ、所持通貨を組み合わせて価値を計算する機能は未実装。
6. ショップ利用後の実消費額を経路後半へ反映するか、イベント処理ターンの実績モデル、未分類特殊イベント辞書は未確定。
7. 実際に通った直前edgeを次回探索へ渡す仕組みは十分に実機検証されていない。折り返しが再発したらbefore_current_node_idの保持を確認する。
8. hit予測の追加開発とデバフカウント再設計は、ユーザーから再開指示があるまで行わない。
9. moveTIPSへの統合は実戦確認完了後まで行わない。

作業原則:

- 変更はtestExtension内だけに限定する。
- Dropbox同期による一時ロックを前提に、ディレクトリ全体を移動しない。
- 既存のユーザー変更を保持する。
- 修正には可能な限り実データ由来のfixtureと回帰テストを追加する。
- 変更後はunit-test-v2.cjs、Manifest参照、JavaScript構文、Dropbox競合コピーを確認する。
- ユーザーへの報告は、結果、変更した優先順位、検証内容、実機で次に見る点を簡潔に示す。

作業開始時に、ドキュメントから把握した現在状態と単体テスト結果を簡潔に報告してください。
```
